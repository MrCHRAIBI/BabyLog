---
phase: 01-foundation-data-layer
plan: 06
type: execute
wave: 5
depends_on: ["01-04", "01-05"]
files_modified:
  - src/core/errors/logger.ts
  - src/core/errors/ErrorBoundary.tsx
  - tests/errors/logger.test.ts
  - plugins/withDataExtractionRules/index.js
  - tests/plugins/data-extraction-rules.test.ts
  - app.json
  - modules/backup-guard/index.ts
  - modules/backup-guard/expo-module.config.json
  - modules/backup-guard/ios/BackupGuardModule.swift
  - modules/backup-guard/android/build.gradle
  - src/core/backupGuard/index.ts
autonomous: true
requirements: [OPS-02]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Une erreur de rendu provoquée en test atterrit dans la table error_log via le logger central (SPEC R6, AC #10)"
    - "Si l'insertion error_log échoue, le logger avale l'erreur au lieu de crasher l'app (SPEC R6, AC #10)"
    - "L'ErrorBoundary racine affiche le fallback brandé D-005 (palette lavande des tokens, dark-OLED ready, message exact « Rien n'est perdu — tes données sont sur cet appareil ») avec les deux actions D-006 : Relancer + Exporter le rapport (fichier local partageable via expo-sharing)"
    - "Le manifest Android mergé porte allowBackup=false et l'attribut dataExtractionRules pointant vers un XML excluant cloud-backup et device-transfer (SPEC R5, AC #7 — OPS-02)"
    - "Le module natif iOS expose excludeFromBackup(path) ET isExcludedFromBackup(path) (read-back — preuve AC #8 in-app) ; le sweep JS flaggue babylog.db, -wal, -shm et tous les fichiers des répertoires MMKV après migration/création, idempotent à chaque boot"
    - "Le plugin Android est idempotent (pas de duplication d'attributs au second prebuild) et honnore le mode introspection"
  artifacts:
    - path: "src/core/errors/logger.ts"
      provides: "logError — insert errorLogRepository (message/code/stack/context uniquement, caps docs/05 719-732), avale ses propres échecs"
    - path: "src/core/errors/ErrorBoundary.tsx"
      provides: "Class boundary racine D-005/D-006 — fallback lavande + Relancer + export rapport"
    - path: "plugins/withDataExtractionRules/index.js"
      provides: "Config plugin : withDangerousMod écrit res/xml/data_extraction_rules.xml + withAndroidManifest ajoute allowBackup=false + dataExtractionRules"
    - path: "modules/backup-guard/"
      provides: "Module Expo local (Swift) : excludeFromBackup / isExcludedFromBackup via URLResourceKey"
    - path: "src/core/backupGuard/index.ts"
      provides: "sweepAfterBoot — flag DB (-wal/-shm via defaultDatabaseDirectory) + répertoires MMKV, idempotent, no-op Android"
    - path: "tests/plugins/data-extraction-rules.test.ts"
      provides: "Test unitaire plugin : XML généré + attributs manifest + idempotence (Pitfall 5)"
  key_links:
    - from: "src/core/errors/logger.ts"
      to: "src/db/repositories/errorLogRepository.ts"
      via: "Seule écriture error_log — try/catch avalant l'échec d'insertion"
      pattern: "errorLogRepository"
    - from: "src/core/backupGuard/index.ts"
      to: "modules/backup-guard/"
      via: "Appel du module natif par fichier : db + -wal + -shm + répertoires MMKV (paths de 01-05)"
      pattern: "excludeFromBackup"
    - from: "app.json"
      to: "plugins/withDataExtractionRules/index.js"
      via: "Entrée plugin locale dans le tableau plugins"
      pattern: "withDataExtractionRules"
  prohibitions:
    - statement: "MUST NOT journaliser de donnée personnelle (prénom bébé, contenu de note) dans error_log ou event"
      status: resolved
      verification: judgment — revue sémantique requise (non mécaniquement checkable) ; mitigation structurelle via API logError bornée à message/code/stack/context (sanitation par conception) + test de sanitation dans tests/errors/logger.test.ts ; docs/05 §11 l'interdit déjà
---

<objective>
Construire la gestion d'erreur racine (logger central + ErrorBoundary brandé D-005/D-006) et les exclusions de backup OS des deux plateformes : plugin Android allowBackup=false + dataExtractionRules (OPS-02), module natif iOS local posant isExcludedFromBackup au runtime sur babylog.db/-wal/-shm et les fichiers MMKV.

Purpose: SPEC R5 + R6 + le cœur d'OPS-02 — aucune donnée bébé ne quitte l'appareil via un canal de backup OS ; les crashes de rendu sont journalisés localement avec un fallback qui rassure au lieu d'un écran blanc.
Output: logger + ErrorBoundary + test, plugin Android + test, module natif iOS + sweep JS.
</objective>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
docs/05-data-model.md (errorLogInsertSchema lignes 719-732)
src/db/repositories/errorLogRepository.ts (01-04)
src/core/storage/mmkv.ts + secureStorage.ts (01-05 — paths et clé)
src/theme/tokens.ts (01-01 — palette lavande D-005)
app.json (état courant — bloc android + plugins à étendre)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logger central + ErrorBoundary racine brandée (D-005/D-006)</name>
  <files>src/core/errors/logger.ts, src/core/errors/ErrorBoundary.tsx, tests/errors/logger.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 719-732 (errorLogInsertSchema : message <= 1000, code <= 120, stack <= 20000, context <= 10000)
    - src/db/repositories/errorLogRepository.ts (01-04 — API d'insertion à consommer)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md (D-005 : fallback lavande dark-OLED-ready + message exact ; D-006 : Relancer + Exporter le rapport fichier local partageable)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (expo-file-system NOUVELLE API File/Paths + expo-sharing ; expo-file-system legacy throw en SDK 57)
    - src/theme/tokens.ts (couleurs de secours — le fallback utilise les tokens, jamais un hex brut)
  </read_first>
  <action>Créer src/core/errors/logger.ts : fonction logError prenant un objet borné aux quatre champs autorisés (message, code, stack, context — types dérivés du schéma errorLog de 01-03) et insérant via errorLogRepository dans un try/catch qui avale toute erreur d'insertion (jamais de re-throw — un logger qui crashe l'app est un anti-sens, AC #10) ; la signature n'offre structurellement AUCUN champ pour du contenu bébé (prénom, note) — la sanitation est par conception d'API, prohibition V7. Créer src/core/errors/ErrorBoundary.tsx : class component React boundary racine — getDerivedStateFromError + componentDidCatch qui appelle logError (message tronqué aux caps, stack, code 'RENDER_ERROR') ; rendu de fallback D-005 : fond utilisant les tokens (dark-OLED ready), message exact « Rien n'est perdu — tes données sont sur cet appareil », deux actions D-006 — bouton Relancer (reset de l'état interne de la boundary pour retenter le rendu) et bouton « Exporter le rapport » qui écrit un fichier de rapport local (nouvelle API expo-file-system : File + Paths.document, nom horodaté, contenu = message/code/stack horodatés, SANS donnée bébé) puis ouvre le partage natif via expo-sharing. Créer tests/errors/logger.test.ts sur vitest : avec un repository factice, une erreur loggée produit une insertion avec les champs autorisés ; un repository qui throw laisse logError retourner normalement sans exception (avaler) ; le rapport exporté ne contient jamais de champ au-delà de message/code/stack/context (assert de sanitation).</action>
  <verify>
    <automated>npx vitest run tests/errors/logger.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou une exception propagée par logError sur échec d'insertion</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit (boundary ou imports expo mal typés)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/errors/logger.test.ts vert : insertion OK, échec d'insertion avalé sans exception (AC #10)
    - La signature publique de logError n'expose que message/code/stack/context (aucun champ contenu bébé possible)
    - ErrorBoundary affiche le message D-005 exact et les deux boutons Relancer / Exporter le rapport (D-005-ctx, D-006-ctx)
    - L'export du rapport utilise la NOUVELLE API expo-file-system (File/Paths) + expo-sharing — aucun import legacy
    - Le fallback référence les tokens de 01-01 (aucun hex codé en dur hors tokens)
  </acceptance_criteria>
  <done>Crash de rendu journalisé localement avec fallback rassurant et export de rapport partageable — D-005/D-006 livrés.</done>
</task>

<task type="auto">
  <name>Task 2: Plugin Android dataExtractionRules + app.json allowBackup (OPS-02)</name>
  <files>plugins/withDataExtractionRules/index.js, tests/plugins/data-extraction-rules.test.ts, app.json</files>
  <read_first>
    - app.json (état courant — bloc android existant à préserver : adaptiveIcon, predictiveBackGestureEnabled ; plugins array-form ; userInterfaceStyle automatic à CONSERVER — Phase 3 s'y branche)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 3 Android : plugin withDangerousMod + withAndroidManifest, XML empty cloud-backup/device-transfer ; Pitfall 5 : idempotence + introspection)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md (D-026 minSdk 33 — pas de legacy full-backup-content nécessaire)
  </read_first>
  <action>Créer plugins/withDataExtractionRules/index.js : config plugin local exportant une fonction qui (1) via withDangerousMod écrit android/app/src/main/res/xml/data_extraction_rules.xml avec des sections vides cloud-backup et device-transfer (tout exclure, auto-documenté) — en gardant le fichier s'il existe déjà avec le contenu attendu (garde d'idempotence : ne pas réécrire/dupliquer) et en court-circuitant proprement en mode introspection (config.modRequest.introspect=true, Pitfall 5) ; (2) via withAndroidManifest ajoute à <application> les attributs android:allowBackup=false et android:dataExtractionRules=@xml/data_extraction_rules — en vérifiant la présence avant ajout pour rester idempotent au second prebuild. minSdk 33 (D-026 via expo-build-properties) rend tout legacy full-backup-content inutile. Mettre à jour app.json : android.allowBackup=false fusionné DANS le bloc android existant (garder adaptiveIcon et predictiveBackGestureEnabled) ; tableau plugins étendu avec expo-iap, expo-build-properties configuré android minSdkVersion 33 (D-026), et le plugin local ./plugins/withDataExtractionRules — sans toucher à expo-router, expo-splash-screen, userInterfaceStyle automatic ni experiments.typedRoutes. Créer tests/plugins/data-extraction-rules.test.ts : exécute le mod du plugin sur un objet config/manifest témoin et assert le XML généré (sections vides cloud-backup + device-transfer) + les deux attributs posés sur application + un second passage qui ne duplique rien (idempotence prouvée par test, Pitfall 5).</action>
  <verify>
    <automated>npx vitest run tests/plugins/data-extraction-rules.test.ts</automated>
    <fails_when>non-zero exit, attribut manifest absent du témoin, XML malformé, ou duplication au second passage</fails_when>
    <automated>node -e "const c=require('./app.json').expo; if(c.android.allowBackup!==false) process.exit(1); const p=JSON.stringify(c.plugins); if(!p.includes('withDataExtractionRules')||!p.includes('expo-iap')||!p.includes('expo-build-properties')) process.exit(1)"</automated>
    <fails_when>exit 1 (allowBackup non false ou entrées plugins manquantes dans app.json)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/plugins/data-extraction-rules.test.ts vert : XML correct + allowBackup=false + dataExtractionRules posés + second passage sans duplication
    - app.json : android.allowBackup===false, plugins contiennent expo-iap, expo-build-properties (minSdkVersion 33) et ./plugins/withDataExtractionRules
    - userInterfaceStyle automatic, typedRoutes, expo-router et le bloc android existant préservés
    - Le plugin honnore introspection (pas d'écriture FS en mode introspect)
  </acceptance_criteria>
  <done>Exclusion Android prête pour le prebuild — la vérification merged-manifest (aapt2) est l'AC device du plan 01-07.</done>
  <reversibility rating="reversible">Config build-time — un revert de app.json + suppression du plugin restaure l'état antérieur sans données en jeu.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Module natif iOS backup-guard + sweep JS (isExcludedFromBackup runtime)</name>
  <files>modules/backup-guard/index.ts, modules/backup-guard/expo-module.config.json, modules/backup-guard/ios/BackupGuardModule.swift, modules/backup-guard/android/build.gradle, src/core/backupGuard/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 3 iOS verbatim : module local create-expo-module --local, Swift setResourceValue isExcludedFromBackupKey ; Pitfall 4 : -wal/-shm créés APRÈS la première écriture → sweep après migrate, ré-idempotent à chaque boot ; Assumption A1 : pas de propagation aux fichiers créés plus tard → flagger chaque fichier)
    - docs.expo.dev/versions/v57.0.0/ (AGENTS.md mandate — modules locaux : create-expo-module --local génère la structure ; expo-sqlite exporte defaultDatabaseDirectory au runtime pour calculer les chemins DB sans les coder en dur)
    - src/core/storage/mmkv.ts (01-05 — paths explicites des instances à énumérer)
    - src/core/database/client.ts (01-03 — nom du fichier DB babylog.db)
  </read_first>
  <action>Générer le module local via npx create-expo-module@latest --local nommé backup-guard (conserver la structure générée : expo-module.config.json, index.ts, ios, android) puis l'adapter : côté Swift, deux fonctions synchrones — excludeFromBackup(path: String) -> Bool qui pose url.setResourceValue(true, forKey: .isExcludedFromBackupKey) avec try? retournant false en cas d'échec, et isExcludedFromBackup(path: String) -> Bool qui relit resourceValues(forKeys: [.isExcludedFromBackupKey]) et retourne le flag (ce read-back est la preuve in-app de l'AC #8 pour le plan 01-07, RESEARCH Open Questions (RESOLVED) Q1) ; côté Android, implémentation no-op retournant true (l'exclusion Android est entièrement build-time via le plugin de la Task 2). Créer src/core/backupGuard/index.ts : sweepAfterBoot() async qui (1) calcule les chemins babylog.db + babylog.db-wal + babylog.db-shm à partir de defaultDatabaseDirectory de expo-sqlite (jamais de chemin codé en dur) et appelle excludeFromBackup sur chacun — les fichiers -wal/-shm peuvent ne pas exister encore : traiter l'échec fichier-absent comme non bloquant mais les re-flagguer au boot suivant (A1 : pas de propagation) ; (2) énumère récursivement les répertoires des deux instances MMKV via leurs paths explicites de 01-05 et flaggue CHAQUE fichier trouvé (agnostique du nommage MMKV — Assumption A2) ; (3) est idempotent (ré-exécution à chaque boot, Pitfall 4) et ne fait rien de bloquant sur Android (Platform guard — les appels natifs y sont no-op). Exporter aussi une fonction de read-back (même liste de fichiers) pour l'AC device iOS de 01-07.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit (types du module local ou imports expo mal résolus)</fails_when>
    <automated>grep -c "isExcludedFromBackup" modules/backup-guard/ios/BackupGuardModule.swift</automated>
    <fails_when>sortie 0 (le Swift ne référence pas la resource key attendue)</fails_when>
    <automated>grep -c "defaultDatabaseDirectory" src/core/backupGuard/index.ts</automated>
    <fails_when>sortie 0 (chemins DB codés en dur au lieu de l'export expo-sqlite)</fails_when>
  </verify>
  <acceptance_criteria>
    - modules/backup-guard expose excludeFromBackup ET isExcludedFromBackup (Swift), Android no-op true
    - src/core/backupGuard calcule les chemins DB via defaultDatabaseDirectory et énumère les répertoires MMKV de 01-05 (aucun chemin codé en dur)
    - Le sweep couvre babylog.db, -wal, -shm et tous les fichiers MMKV, est idempotent et non bloquant sur fichiers absents
    - La fonction de read-back est exportée pour la vérification device iOS (01-07, AC #8)
    - npx tsc --noEmit vert avec le module local intégré
  </acceptance_criteria>
  <done>Exclusions iOS opérationnelles au runtime — la vérification device (read-back + EAS build) est l'AC du plan 01-07.</done>
  <reversibility rating="reversible">Module local isolé — suppression du dossier + retrait de l'appel au boot restaure l'état antérieur.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exception runtime → error_log | Les crashs contiennent potentiellement du contexte sensible |
| OS (backup cloud Android/iCloud) → fichiers de données | DB + MMKV ne doivent jamais être éligibles au backup |
| prebuild → manifest Android | Le plugin transforme le manifest à chaque build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06a | Information Disclosure | Donnée bébé dans error_log/event | high | mitigate | API logError bornée à message/code/stack/context (aucun champ contenu possible) + test de sanitation ; revue sémantique requise (prohibition R6, verification judgment — portée en revue de phase) |
| T-01-06b | Information Disclosure | DB/MMKV exfiltrées via auto-backup Android | high | mitigate | allowBackup=false + dataExtractionRules (cloud-backup et device-transfer vides) ; vérification aapt2 sur l'APK mergé au plan 01-07 (AC #7, OPS-02) |
| T-01-06c | Information Disclosure | DB/-wal/-shm/MMKV dans l'iCloud backup | high | mitigate | Sweep runtime isExcludedFromBackup après migrate + création MMKV, ré-idempotent à chaque boot (Pitfall 4 / A1) ; read-back in-app comme preuve AC #8 (01-07) |
| T-01-06d | Tampering | Plugin non idempotent corrompant le manifest | medium | mitigate | Gardes d'idempotence + mode introspection honoré + test unitaire à deux passages (Pitfall 5) |
| T-01-06e | Denial of Service | Logger qui crash l'app en cascade | medium | mitigate | try/catch avalant l'échec d'insertion (AC #10) ; boundary qui journalise puis rend un fallback, jamais de re-throw |
</threat_model>

<verification>
- npx vitest run tests/errors tests/plugins vert
- npx tsc --noEmit vert (boundary, sweep, module local intégrés)
- app.json porte allowBackup=false + les trois entrées plugins ; plugin idempotent prouvé par test
- Sweep iOS couvre DB + sidecars + MMKV avec read-back exporté
</verification>

<success_criteria>
Les erreurs de rendu sont journalisées localement avec un fallback brandé D-005/D-006, et les deux canaux de backup OS sont fermés : Android au build (allowBackup=false + dataExtractionRules, OPS-02), iOS au runtime (isExcludedFromBackup sur chaque fichier de données) — vérifications device portées par 01-07.
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-06-SUMMARY.md` quand done
</output>
