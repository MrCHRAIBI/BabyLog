---
phase: 01-foundation-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eas.json
  - babel.config.js
  - metro.config.js
  - tailwind.config.ts
  - src/global.css
  - src/theme/tokens.ts
  - src/services/.gitkeep
  - src/app/_layout.tsx
  - src/app/index.tsx
autonomous: false
requirements: [OPS-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Le corrected-stack complet est installé aux versions RESEARCH (expo-sqlite ~57.0.3, react-native-mmkv ^4.3.2, react-native-nitro-modules, react-native-quick-crypto ^1.1.7, nativewind ^4.2.7, tailwindcss 3.4.x, zod ^4.6.5, i18next ^26.4.2, zustand ^5.0.15, expo-dev-client, expo-iap ^5.6.2) et `npm ls` résout sans conflit peer bloquant"
    - "Aucun fichier démo du scaffold ne reste : src/app ne contient plus que _layout.tsx et index.tsx minimaux qui compilent (SPEC R9)"
    - "La primaire lavande #8893FE n'apparaît que dans src/theme/tokens.ts, source unique référencée par tailwind.config.ts (SPEC R8)"
    - "eas.json définit le profil development (developmentClient true, distribution internal) avec cli >= 19.1.0 (D-001-ctx)"
    - "package.json n'embarque aucune dépendance cloud/SaaS/analytics/pub et aucun des deux paquets interdits D-024 (audit zero-cloud de 01-02 restera vert)"
  artifacts:
    - path: "package.json"
      provides: "Dépendances corrected-stack + scripts test (vitest run) et android:dev (expo run:android)"
    - path: "eas.json"
      provides: "Profil development EAS (D-001-ctx)"
    - path: "babel.config.js"
      provides: "babel-preset-expo jsxImportSource nativewind + preset nativewind/babel"
    - path: "metro.config.js"
      provides: "withNativeWind(config, { input: './src/global.css' })"
    - path: "tailwind.config.ts"
      provides: "preset nativewind/preset + theme.extend.colors importés depuis src/theme/tokens.ts (aucun hex dupliqué)"
    - path: "src/theme/tokens.ts"
      provides: "Colors (lavande #8893FE, dark OLED #000000) + Spacing en `as const` + types dérivés — source unique docs/06"
    - path: "src/app/_layout.tsx"
      provides: "Layout racine minimal compilable (scaffold démo purgé) — sera étendu par 01-07"
    - path: "src/app/index.tsx"
      provides: "Écran minimal compilable — sera étendu par 01-07"
    - path: "src/services/.gitkeep"
      provides: "Couche services vide (D-008-ctx) — placeholder à peupler par les phases suivantes"
  key_links:
    - from: "metro.config.js"
      to: "src/global.css"
      via: "withNativeWind input css (directives @tailwind en tête du fichier)"
      pattern: "withNativeWind"
    - from: "tailwind.config.ts"
      to: "src/theme/tokens.ts"
      via: "import des tokens (colors) — aucune seconde déclaration de palette"
      pattern: "theme/tokens"
---

<objective>
Installer le corrected stack (D-024) sur le scaffold Expo 57, purger les fichiers démo du template, poser les design tokens en source unique avec le wiring NativeWind/tailwindcss 3.4.x, et créer la config EAS du workflow hybride (D-001-ctx). Le repo compile (`tsc` strict) après ce plan et devient la base propre sur laquelle les plans 01-02..01-07 construisent.

Purpose: SPEC R8/R9/R10 + contrainte dev build — MMKV et quick-crypto ne chargent pas sous Expo Go ; le scaffold démo doit disparaître avant que la couche données n'y prenne racine ; la palette lavande doit exister en source unique avant tout écran (ErrorBoundary D-005 en 01-05, skeleton en 01-06).
Output: package.json mise à jour + lockfile, eas.json, wiring NativeWind (babel/metro/tailwind/global.css), src/theme/tokens.ts, _layout/index minimaux, scaffold démo supprimé.
</objective>

<assumption_delta_decision>
noun: "translation source" / decision: "no-change" / rationale: "EN fallback locale is a deterministic single-source i18n pattern, not a second source of truth; expo-iap is a locked library selection (D-024), not an identity-model change. No promote/add-alongside needed; no invariant test required."
</assumption_delta_decision>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-PATTERNS.md
docs/06-ui-ux-design.md

Sections RESEARCH à relire : Package Legitimacy Audit (verdicts SUS + dispositions), Standard Stack / Installation (bloc verbatim), Code Examples (NativeWind wiring, eas.json), Anti-Patterns (tailwind 4 / NativeWind 5 interdits, legacy expo-file-system).
Sections PATTERNS à relire : pattern `package.json` (scripts), pattern `app.json` (plugins), pattern `src/global.css`, pattern `vitest.config.ts` (alias miroir — informatif ici).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirmer batch les overrides de légitimité des paquets SUS (supply chain)</name>
  <files>(aucun — décision humaine avant toute installation)</files>
  <what-built>La recherche a audité les 19 paquets à installer (RESEARCH.md, section Package Legitimacy Audit, 2026-09-18) : aucun [SLOP], aucun postinstall script, tous les verdicts SUS sont des flags "too-new" (latest publié < 7 jours) levés par provenance first-party (expo/* via bundledNativeModules.json), URL de repo officielle, téléchargements à l'échelle million, et absence de postinstall. Les paquets concernés : expo-sqlite, expo-iap, expo-dev-client, expo-secure-store, expo-crypto, expo-localization, expo-build-properties, expo-file-system, nativewind, zod, i18next + react-i18next, vitest, react-native-nitro-modules.</what-built>
  <how-to-verify>1. Ouvrir RESEARCH.md section Package Legitimacy Audit et parcourir la colonne Disposition. 2. Confirmer l'override batch des verdicts SUS ("Approved"). 3. Toute objection nommée → le plan s'arrête et la substitution se prend dans la table Alternatives Considered (remplacement ou suppression du paquet) avant la Task 2. Aucune installation ne doit avoir eu lieu avant cette confirmation.</how-to-verify>
  <resume-signal>Taper "approved" pour confirmer l'override batch et lancer les installations, ou nommer le(s) paquet(s) à substituer/supprimer</resume-signal>
  <done>Override batch confirmé par l'utilisateur, ou substitution décidée et journalisée dans le SUMMARY avant tout npm/expo install.</done>
</task>

<task type="auto">
  <name>Task 2: Installer le corrected stack + scripts npm + eas.json</name>
  <precondition>Task 1 confirmée (override batch légitimité accepté) — aucune install ne précède la Task 1.</precondition>
  <files>package.json, package-lock.json, eas.json</files>
  <read_first>
    - package.json (état courant — scripts à préserver, main expo-router/entry, private true)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (bloc Installation verbatim + eas.json Code Example + colonnes versions Standard Stack)
  </read_first>
  <action>Exécuter les trois commandes d'installation du bloc Installation de RESEARCH.md, dans cet ordre : (1) npx expo install expo-sqlite expo-file-system expo-sharing expo-secure-store expo-crypto expo-localization expo-build-properties expo-dev-client expo-iap — les modules first-party DOIVENT passer par expo install pour recevoir les pins SDK 57 exacts (jamais npm direct pour eux) ; (2) npm install react-native-mmkv react-native-nitro-modules react-native-quick-crypto react-native-quick-base64 nativewind zustand zod i18next react-i18next ; (3) npm install -D tailwindcss@~3.4.19 prettier-plugin-tailwindcss vitest better-sqlite3 @types/better-sqlite3 — tailwind épinglé à la ligne 3.4 (jamais 4.x, NativeWind 4 ne le supporte pas). Ajouter deux scripts à package.json sans toucher aux existants : test = vitest run ; android:dev = expo run:android (D-001-ctx — Android reste en build local). Créer eas.json avec cli.version >= 19.1.0 et un profil build.development portant developmentClient true et distribution internal (RESEARCH Code Example eas.json — forme minimale, pas de profils preview/production en Phase 1). Ne pas installer react-native-iap ni crypto-js (D-024 : le premier est archivé et épingle un nitro concurrent, le second est discontinué). Conserver main = expo-router/entry et private = true.</action>
  <verify>
    <automated>npm ls --depth=0</automated>
    <fails_when>non-zero exit (arbre de dépendances non résolu / conflit peer), ou un des paquets installés absent de la liste</fails_when>
    <automated>node -e "const v=require('tailwindcss/package.json').version; if(!v.startsWith('3.4.')) process.exit(1); console.log(v)"</automated>
    <fails_when>exit 1 (tailwindcss hors ligne 3.4) ou version non affichée</fails_when>
    <automated>test -f eas.json && node -e "const c=require('./eas.json'); if(!(c.cli&&c.cli.version&&c.build&&c.build.development&&c.build.development.developmentClient===true&&c.build.development.distribution==='internal')) process.exit(1)"</automated>
    <fails_when>exit 1 (eas.json absent ou profil development incomplet)</fails_when>
  </verify>
  <acceptance_criteria>
    - npm ls --depth=0 exit 0 et liste react-native-mmkv, react-native-nitro-modules, react-native-quick-crypto, nativewind, zod, zustand, i18next, react-i18next, vitest, better-sqlite3
    - Version tailwindcss affichée commence par 3.4.
    - eas.json parse en JSON et porte cli.version, build.development.developmentClient=true, build.development.distribution=internal
    - package.json contient les scripts test et android:dev, et conserve main=expo-router/entry, private=true, lint=expo lint
  </acceptance_criteria>
  <done>Corrected stack installé aux pins RESEARCH, scripts et eas.json en place, lockfile commité.</done>
  <reversibility rating="reversible">Choix de versions épinglées documenté — un npm install alternatif ne verrouille rien structurellement.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Purger le scaffold démo + wiring NativeWind + tokens source unique</name>
  <files>babel.config.js, metro.config.js, tailwind.config.ts, src/global.css, src/theme/tokens.ts, src/services/.gitkeep, src/app/_layout.tsx, src/app/index.tsx (suppressions : src/app/explore.tsx, src/components/, src/constants/theme.ts, src/hooks/, scripts/reset-project.js)</files>
  <read_first>
    - src/app/_layout.tsx (contenu courant — pattern wiring point d'entrée à conserver : preventAutoHideAsync module scope, default export, imports @/)
    - src/app/index.tsx (contenu courant — pattern écran : default export + StyleSheet.create en bas)
    - src/global.css (état courant — le bloc :root existe déjà)
    - src/constants/theme.ts (convention tokens as const + types dérivés — le fichier lui-même est supprimé, on copie la CONVENTION)
    - .planning/phases/01-foundation-data-layer/01-PATTERNS.md (sections purge + global.css + tokens + _layout/index rewrite)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Code Examples NativeWind verbatim)
    - docs/06-ui-ux-design.md (palette lavande complète, dark OLED, espacements — source des valeurs de tokens)
  </read_first>
  <action>Supprimer tous les fichiers démo du template : src/app/explore.tsx ; tout src/components/ (animated-icon.tsx/.web.tsx/.module.css, app-tabs.tsx/.web.tsx, external-link.tsx, hint-row.tsx, themed-text.tsx, themed-view.tsx, ui/collapsible.tsx, web-badge.tsx) ; src/constants/theme.ts ; src/hooks/ (use-color-scheme.ts, use-color-scheme.web.ts, use-theme.ts) ; scripts/reset-project.js ET son entrée reset-project dans package.json scripts (le script recréerait le template — ne jamais l'exécuter). Créer babel.config.js et metro.config.js exactement selon les Code Examples NativeWind de RESEARCH.md (presets babel-preset-expo avec jsxImportSource nativewind + nativewind/babel ; withNativeWind avec input pointant le CSS global sous src/). Ajouter les trois directives @tailwind (base, components, utilities) en tête de src/global.css, au-dessus du bloc :root existant. Créer src/theme/tokens.ts : constante Colors en as const (light + dark) avec la primaire lavande du doc 06 et les fonds dark OLED purs, constante Spacing en as const, types dérivés par keyof typeof — c'est la source unique de toute la palette (SPEC R8). Créer tailwind.config.ts (tailwind 3.4 supporte la config TS) qui importe les couleurs depuis src/theme/tokens.ts dans theme.extend.colors avec le preset nativewind et content couvrant src/**/*.{ts,tsx} — aucun hex ne doit être redéclaré dans la config. Réécrire src/app/_layout.tsx en version minimale : import du CSS global, SplashScreen.preventAutoHideAsync() en scope module (mécanisme conservé du scaffold), default export d'un composant racine trivial sans aucun import démo. Réécrire src/app/index.tsx en écran minimal placeholder (Text + StyleSheet.create) sans import démo. Créer la couche vide src/services/ avec un .gitkeep (couche nommée par D-008-ctx — aucun code cette phase, les phases suivantes la peuplent ; PATTERNS.md la classe comme créée cette phase). L'extension réelle de _layout/index est le plan 01-07 — ici l'objectif est uniquement que l'arbre compile après purge.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit (erreurs TypeScript — imports démo orphelins ou tokens mal typés)</fails_when>
    <automated>test ! -e src/app/explore.tsx && test ! -d src/components && test ! -d src/hooks && test ! -e src/constants/theme.ts && echo PURGED</automated>
    <fails_when>exit non-zero ou sortie différente de PURGED (un fichier démo survit)</fails_when>
    <automated>node -e "const s=require('fs').readFileSync('src/global.css','utf8'); if(!/@tailwind base/.test(s)||!/@tailwind components/.test(s)||!/@tailwind utilities/.test(s)) process.exit(1)"</automated>
    <fails_when>exit 1 (directives @tailwind absentes du CSS global)</fails_when>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit passe en strict sur tout l'arbre
    - src/app ne contient que _layout.tsx et index.tsx ; src/components, src/hooks, src/constants, src/app/explore.tsx et scripts/reset-project.js n'existent plus
    - _layout.tsx et index.tsx ne référencent aucun module supprimé (tsc le prouve) et gardent le pattern default export + imports @/
    - src/theme/tokens.ts exporte Colors et Spacing en as const avec types dérivés ; la valeur hexadécimale de la primaire lavande du doc 06 y figure exactement une fois dans tout src/
    - src/services/.gitkeep existe (couche D-008-ctx présente, vide)
    - tailwind.config.ts importe ses couleurs depuis src/theme/tokens.ts (aucun hex dupliqué dans la config)
    - global.css porte les trois directives @tailwind en tête
  </acceptance_criteria>
  <done>Scaffold purgé, NativeWind câblé, tokens en source unique, arbre compile — prêt pour la couche données.</done>
  <!-- planner-discipline-allow: #8893FE -->
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | Tout paquet installé entre ici ; vecteur supply-chain (slopsquatting, postinstall malveillant) |
| build config → artefact natif | babel/metro/tailwind/eas.json façonnent le bundle et le dev build EAS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm/expo installs (19 paquets) | high | mitigate | Audit de légitimité RESEARCH effectué (verdicts + dispositions journalisés) + checkpoint batch blocking-human en Task 1 AVANT install + installs first-party via npx expo install (pins SDK) + absence de postinstall vérifiée + test zero-cloud allowlist (01-02) qui restera vert sur ce package.json |
| T-01-01 | Information Disclosure | package.json (dépendances cloud) | high | mitigate | Aucune dépendance cloud/SaaS/analytics/pub installée (contrainte SPEC R10) — audit automatisé fail-first livré en 01-02 |
| T-01-02 | Tampering | eas.json / profils de build | low | accept | Profil development internal uniquement ; aucune distribution store en Phase 1 — surface d'attaque minimale |
</threat_model>

<verification>
- npm ls --depth=0 résout ; versions aux pins RESEARCH ; tailwindcss en 3.4.x
- npx tsc --noEmit vert en strict
- Aucun fichier démo restant ; _layout + index minimaux compilent
- eas.json profil development complet (vérifié programmatiquement en Task 2)
</verification>

<success_criteria>
Le corrected stack est installé et câblé, le scaffold est purgé, les tokens sont en source unique, la config EAS existe — le repo compile et sert de base propre aux plans 01-02..01-07, sans aucune dépendance cloud (D-024 respecté, base du test zero-cloud de 01-02).
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-01-SUMMARY.md` quand done
</output>
