---
phase: 01-foundation-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/db/repositories/babyProfileRepository.ts
  - src/db/repositories/logEventRepository.ts
  - src/db/repositories/timerRepository.ts
  - src/db/repositories/fileExportRepository.ts
  - src/db/repositories/eventRepository.ts
  - src/db/repositories/errorLogRepository.ts
  - tests/repositories/babyProfileRepository.test.ts
  - tests/repositories/logEventRepository.test.ts
  - tests/repositories/timerRepository.test.ts
  - tests/repositories/fileExportRepository.test.ts
  - tests/repositories/eventRepository.test.ts
  - tests/repositories/errorLogRepository.test.ts
autonomous: true
requirements: [OPS-02]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Les six repositories (babyProfile, logEvent, timer, fileExport, event, errorLog) passent le harness Vitest in-memory SQLite avec écritures Zod-validées (SPEC R3, AC #3)"
    - "Les requêtes de liste sur DB vide retournent un tableau vide sans erreur, pour chaque repository (SPEC R3, AC #4)"
    - "Deux lignes au même created_at ne produisent ni doublon ni saut de page — tie-break par id sur la pagination keyset (created_at, id) (SPEC R3, AC #4)"
    - "L'ordre de liste est déterministe : created_at DESC puis id DESC, y compris aux ties (SPEC R3, AC #4)"
    - "Aucun SQL n'existe hors des repositories et de la couche migrations, et la pagination n'utilise jamais le mot-clé de saut de page interdit — l'audit tests/sql-isolation.test.ts de 01-02 reste vert après l'ajout des six repositories"
    - "Toute opération multi-écritures s'exécute dans une transaction exclusive dont les requêtes passent par le handle txn (rollback prouvé par test)"
  artifacts:
    - path: "src/db/repositories/babyProfileRepository.ts"
      provides: "listAll / getById / create / update / delete / countProfiles — SQL exact docs/05 lignes 734-849"
    - path: "src/db/repositories/logEventRepository.ts"
      provides: "CRUD + getTimelineDay + getRecentWindow + getStatsByRange + completeTimerAndCreateEvent — docs/05 lignes 887-1331 + requêtes chaudes §9"
    - path: "src/db/repositories/timerRepository.ts"
      provides: "getActiveByProfile + start/replace atomique + clear — docs/05 lignes 1332-1431"
    - path: "src/db/repositories/fileExportRepository.ts"
      provides: "Journal des exports (kind/status/period) — docs/05 lignes 1432-1608"
    - path: "src/db/repositories/eventRepository.ts"
      provides: "Analytics local inerte (allowlist noms) — docs/05 lignes 1609-1716"
    - path: "src/db/repositories/errorLogRepository.ts"
      provides: "Insertion error_log consommée par le logger 01-05 — docs/05 lignes 1717-1792"
  key_links:
    - from: "src/db/repositories/*.ts"
      to: "src/db/schemas/*.ts"
      via: "Zod parse des inserts avant runAsync + parse des rows lus (direction D-042 : schemas ← repositories)"
      pattern: "from '@/db/schemas"
    - from: "src/db/repositories/logEventRepository.ts"
      to: "docs/05-data-model.md §9"
      via: "getTimelineDay utilise idx_log_event_timeline avec ORDER BY started_at DESC, id DESC LIMIT 200 — aucun SCAN attendu"
      pattern: "idx_log_event_timeline"
---

<objective>
Implémenter les six repositories de docs/05 §8 — SQL exclusif, écritures Zod-validées, pagination keyset (created_at, id), transactions sur le handle txn — et leur suite de tests sur le harness in-memory : listes vides, ties keyset sans doublon ni saut, rollback de transaction, round-trips complets.

Purpose: SPEC R3 (AC #3/#4) — c'est le contrat que les phases 2-6 consommeront ; le SQL y est confiné définitivement (prohibition R3, audit 01-02).
Output: 6 repositories + 6 fichiers de tests verts sur le harness.
</objective>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
docs/05-data-model.md (§8 règle commune lignes 413-421 ; repositories : babyProfile 734, logEvent 887, timer 1332, fileExport 1432, event 1609, errorLog 1717 ; §9 requêtes chaudes lignes 1793-1905)
tests/harness/sqliteHarness.ts (créé en 01-02 — surface DatabaseLike à consommer)
src/db/schemas/ (créés en 01-02 — imports des schémas feuilles)
</context>

<tasks>

<task type="auto">
  <name>Task 1: babyProfileRepository + logEventRepository + tests</name>
  <files>src/db/repositories/babyProfileRepository.ts, src/db/repositories/logEventRepository.ts, tests/repositories/babyProfileRepository.test.ts, tests/repositories/logEventRepository.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 734-886 (babyProfileRepository — EXEMPLAIRE : SQL exact par méthode, keyset, countProfiles) et lignes 887-1331 (logEventRepository) et lignes 1798-1905 (requêtes chaudes §9 : getTimelineDay, completeTimerAndCreateEvent, getRecentWindow, getStatsByRange)
    - docs/05-data-model.md lignes 413-421 (règle commune — Zod avant écriture, keyset created_at+id, transaction pour toute multi-écriture)
    - tests/harness/sqliteHarness.ts + tests/schemas/boundaries.test.ts (patterns harness + fixtures profil)
    - src/db/schemas/babyProfile.ts, src/db/schemas/logEvent.ts (contrats à importer)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pitfall 1 : requêtes de transaction sur le handle txn)
  </read_first>
  <action>Créer les deux repositories en suivant le SQL exact et les signatures de docs/05 §8 (l'exemplaire babyProfile lignes 734-849 fixe le style : chaque méthode prend un DatabaseLike, parse l'insert via le schéma feuille avant exécution, parse chaque row lue au retour, et la pagination de liste utilise le curseur (created_at, id) en DESC avec tie-break id — jamais le mot-clé de saut de page interdit). babyProfileRepository : listAll, getById, create, update, delete, countProfiles tels que documentés. logEventRepository : insert, getById, update, soft delete (deleted_at), getTimelineDay (requête chaude 2 : filtre baby_profile_id + deleted_at IS NULL + fenêtre started_at, ORDER BY started_at DESC id DESC LIMIT 200, index idx_log_event_timeline), getRecentWindow (curseur keyset — requête chaude 3 consommée par la couche entitlements en Phase 4), getStatsByRange, et la multi-écriture transactionnelle du doc (compléter un timer + créer l'événement) exécutée dans withExclusiveTransactionAsync avec toutes les requêtes sur le handle txn. Tests sur le harness : liste vide renvoie un tableau vide sans erreur ; create → read round-trip complet champs à champs ; rejet Zod (nom vide, amount hors cap) ne touche pas la DB ; deux événements insérés au même created_at explicite se listent sans doublon ni ligne sautée sur deux pages consécutives du curseur (jeu de 5 lignes, page size 2, y compris tie sur frontière de page) ; ordre déterministe created_at DESC puis id DESC ; la multi-écriture transactionnelle roll back intégralement quand la seconde écriture échoue (provocation : violer un CHECK sur l'écriture 2 → aucune trace de l'écriture 1).</action>
  <verify>
    <automated>npx vitest run tests/repositories/babyProfileRepository.test.ts tests/repositories/logEventRepository.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou "0 passed" dans le résumé</fails_when>
  </verify>
  <acceptance_criteria>
    - Les deux fichiers de tests sont verts sur le harness (SQLite réel, PRAGMAs rejoués)
    - Liste vide renvoie un tableau vide pour listAll et getTimelineDay sans exception
    - Le scénario de ties (5 lignes, 2 au même created_at, pagination taille 2) ne perd ni ne duplique aucune ligne entre pages
    - Le rollback transactionnel laisse zéro trace de la première écriture après échec de la seconde
    - Aucune chaîne SQL n'apparaît hors de ces fichiers repositories (l'audit 01-02 le confirme)
  </acceptance_criteria>
  <done>Deux premiers repositories conformes docs/05, keyset et transactions prouvés par tests.</done>
</task>

<task type="auto">
  <name>Task 2: timerRepository + fileExportRepository + tests</name>
  <files>src/db/repositories/timerRepository.ts, src/db/repositories/fileExportRepository.ts, tests/repositories/timerRepository.test.ts, tests/repositories/fileExportRepository.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 1332-1431 (timerRepository) et lignes 1432-1608 (fileExportRepository) et lignes 1793-1805 (requête chaude 1 getActiveByProfile — budget < 1 ms)
    - docs/05-data-model.md lignes 221-236 (DDL timer_state UNIQUE baby_profile_id ; file_export kind/status/period CHECKs)
    - src/db/schemas/timerState.ts, src/db/schemas/fileExport.ts (contrats 01-02)
  </read_first>
  <action>Créer timerRepository selon docs/05 : getActiveByProfile (requête chaude 1 — SELECT sur l'index unique baby_profile_id, retour null si absent), le démarrage d'un timer qui REMPLACE proprement tout timer actif existant en une transaction exclusive (suppression de l'ancien + insertion du nouveau sur le handle txn — un seul timer actif par profil, contrainte UNIQUE), et la suppression du timer actif à l'arrêt. Créer fileExportRepository : create (kind pdf_pediatrician/emergency_doctor/backup/co_parent_transfer, status started par défaut), updateStatus (completed/failed), les listes keyset (globale et par profil, curseur created_at+id DESC). Tests sur le harness : getActiveByProfile sur DB vide renvoie null sans erreur ; démarrer un second timer remplace le premier (une seule ligne au final, l'ancienne disparaît) et l'opération est atomique (échec provoqué de la seconde écriture → l'ancien timer reste intact) ; fileExport : cycle started → completed et started → failed persistés correctement, liste vide renvoie un tableau vide, keyset par profil sans doublon ni saut sur ties created_at, rejet Zod d'un kind inconnu.</action>
  <verify>
    <automated>npx vitest run tests/repositories/timerRepository.test.ts tests/repositories/fileExportRepository.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou "0 passed" dans le résumé</fails_when>
  </verify>
  <acceptance_criteria>
    - Les deux fichiers de tests sont verts sur le harness
    - getActiveByProfile renvoie null sur DB vide et le profil actif sinon (une seule ligne, contrainte UNIQUE respectée)
    - Le remplacement de timer est atomique : échec simulé → ancien timer conservé, aucun état intermédiaire
    - fileExport persiste started/completed/failed et ses listes keyset gèrent les ties created_at sans doublon ni saut
  </acceptance_criteria>
  <done>Timer atomique et journal d'exports conformes docs/05, testés sur le harness.</done>
</task>

<task type="auto">
  <name>Task 3: eventRepository + errorLogRepository + tests + run d'audit complet</name>
  <files>src/db/repositories/eventRepository.ts, src/db/repositories/errorLogRepository.ts, tests/repositories/eventRepository.test.ts, tests/repositories/errorLogRepository.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 1609-1716 (eventRepository) et lignes 1717-1792 (errorLogRepository) et lignes 2038-2124 (allowlist analytics §11)
    - src/db/schemas/event.ts, src/db/schemas/errorLog.ts (allowlist enum + caps message 1000 / code 120 / stack 20000 / context 10000)
    - tests/sql-isolation.test.ts (audit 01-02 — doit rester vert une fois les 6 repositories en place)
  </read_first>
  <action>Créer eventRepository (analytics local INERTE — la table et le repository existent, aucun événement n'est émis en Phase 1, hors scope Phase 7) : insert validé par l'allowlist enum des noms du §11 (rejet de tout nom hors liste), listes keyset created_at+id DESC, count par nom. Créer errorLogRepository : insert (message/code/stack/context avec caps du schéma 01-02), listes keyset, purge éventuelle documentée dans docs/05 si présente (sinon s'en tenir aux méthodes du doc). Tests sur le harness : insert event avec nom allowlisté puis rejet d'un nom hors allowlist ; liste vide renvoie un tableau vide ; errorLog round-trip d'une entrée aux caps exactes (message 1000, stack 20000) et rejet au-delà ; keyset ties sans doublon ni saut. Terminer par la suite complète : npx vitest run sur tout tests/ — les 6 repositories + audits + migrations + schémas verts ensemble.</action>
  <verify>
    <automated>npx vitest run tests/repositories/eventRepository.test.ts tests/repositories/errorLogRepository.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou "0 passed" dans le résumé</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit ou tout fichier de test en échec dans le résumé final (suite complète : audits + migrations + schémas + 6 repositories)</fails_when>
  </verify>
  <acceptance_criteria>
    - eventRepository rejette tout nom d'événement hors allowlist §11 et accepte ceux de la liste
    - errorLogRepository accepte une entrée aux caps exactes et rejette au-delà (message > 1000, code > 120)
    - Listes vides renvoient un tableau vide pour les deux repositories
    - La suite complète npm test est verte : 3 audits + idempotence + boundaries + 6 repositories
    - L'audit d'isolation SQL reste vert avec les 6 repositories en place (SQL confiné, keyset partout)
  </acceptance_criteria>
  <done>Six repositories complets, suite de tests phase verte de bout en bout — contrat de données prêt pour stores, logger et boot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| appelant (hooks/screens/services) → repository | Seule porte d'entrée SQL de l'app ; paramètres non dignes de confiance |
| paramètres repository → SQLite | Toute valeur passe par Zod puis binding paramétré |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03a | Tampering | Injection SQL via construction de requêtes | high | mitigate | SQL exclusivement dans les repositories (prohibition R3, audit 01-02) ; valeurs exclusivement via bindings paramétrés runAsync/getAllAsync — execAsync réservé au DDL/PRAGMAs fixes ; audit fail-first + test de rejet Zod en amont |
| T-01-03b | Elevation of Privilege | Contournement de la validation Zod | high | mitigate | Chaque méthode parse l'insert via le schéma feuille avant exécution ; tests de rejet prouvent que rien n'atteint la DB sans validation |
| T-01-03c | Repudiation | État partiel après multi-écriture | medium | mitigate | Multi-écritures en withExclusiveTransactionAsync sur le handle txn ; test de rollback intégral (échec de la 2e écriture → zéro trace de la 1re) |
| T-01-03d | Information Disclosure | Fuite de données via pagination non bornée | low | accept | LIMIT borné + curseur keyset sur index ; aucun SCAN (requêtes chaudes §9 indexées) |
</threat_model>

<verification>
- npx vitest run tests/repositories/ vert (6 fichiers)
- npm test (suite complète phase) vert : audits + migrations + schémas + repositories
- Scénarios AC #4 couverts : listes vides → tableau vide, ties created_at → ni doublon ni saut, ordre déterministe
</verification>

<success_criteria>
Les six repositories de docs/05 §8 sont implémentés au SQL exact, Zod-validés, keyset-only, transactionnels — la suite de tests phase entière est verte et les prohibitions R3 (isolation SQL + pagination) restent prouvées par l'audit 01-02.
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md` quand done
</output>
