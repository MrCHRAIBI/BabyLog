---
phase: 01-foundation-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/core/database/client.ts
  - src/core/database/migrations/runner.ts
  - src/core/database/migrations/m001.ts
  - tests/migrations/idempotency.test.ts
  - src/db/schemas/primitives.ts
  - src/db/schemas/babyProfile.ts
  - src/db/schemas/logEvent.ts
  - src/db/schemas/timerState.ts
  - src/db/schemas/fileExport.ts
  - src/db/schemas/event.ts
  - src/db/schemas/errorLog.ts
  - src/db/schemas/backup.ts
  - src/db/schemas/analytics.ts
  - src/db/schemas/settings.ts
  - tests/schemas/boundaries.test.ts
autonomous: true
requirements: [OPS-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Re-exécuter le runner sur une DB déjà migrée n'applique aucun changement et laisse exactement une ligne schema_version=1 (SPEC R2, AC #2)"
    - "Le DDL m001 crée exactement 7 tables, 10 index, 6 triggers, avec le CHECK note_text assoupli par 05-addendum-01 (longueur <= 2000 sur tout type, sans exclusion de type) et les enums diaper_type/side verrouillés"
    - "Caps Zod aux limites : note_text 2000 caractères acceptés / 2001 rejetés ; amount_ml 5000 accepté / 5001 rejeté — côté Zod ET côté CHECK SQLite via le harness (SPEC R3, AC #15)"
    - "Round-trip UTF-8/emoji dans note_text : une note de 2000 caractères contenant emoji + accents insérée puis relue via le harness est identique octet pour octet (AC #15, moitié SQLite)"
    - "Les modules feuilles de src/db/schemas/ n'importent que zod (D-042) — gate grep vert"
    - "Une fois client/runner/m001 en place, les trois audits du plan 01-02 restent verts : m001 est dans la zone sanctionnée de l'audit sql-isolation et passe l'audit migrations-non-destructives (qui bascule du mode fixtures au scan du DDL réel)"
  artifacts:
    - path: "src/core/database/client.ts"
      provides: "openDatabaseAsync('babylog.db') + séquence PRAGMAs execAsync + type DatabaseLike partagé"
    - path: "src/core/database/migrations/runner.ts"
      provides: "runMigrations(db) — lecture schema_version, application en 1 transaction par migration, idempotent"
    - path: "src/core/database/migrations/m001.ts"
      provides: "DDL docs/05 §6 (162-347) verbatim amendé addendum — 7 tables / 10 index / 6 triggers"
    - path: "src/db/schemas/"
      provides: "10 modules feuilles Zod (D-042) : primitives + 6 tables + backup/analytics/settings, imports zod uniquement"
    - path: "tests/migrations/idempotency.test.ts"
      provides: "Preuve idempotence runner + comptage tables/index/triggers — exerce le harness 01-02 sur le code réel"
    - path: "tests/schemas/boundaries.test.ts"
      provides: "Caps Zod + CHECK (note 2000/2001, amount 5000/5001, enums) + round-trip emoji note_text"
  key_links:
    - from: "tests/harness/sqliteHarness.ts (01-02)"
      to: "src/core/database/migrations/m001.ts"
      via: "le harness exécute le DDL m001 réel via le runner réel (mêmes SQL, mêmes PRAGMAs rejoués) — premier exercice de l'import créé en 01-02"
      pattern: "runMigrations|M001"
    - from: "src/db/schemas/logEvent.ts"
      to: "tests/schemas/boundaries.test.ts"
      via: "les caps testés importent les schémas feuilles (D-042 : zod uniquement)"
      pattern: "from '@/db/schemas"
  prohibitions:
    - statement: "MUST NOT contenir de SQL destructif (DROP/DELETE de données, ALTER drop) dans toute migration publiée — m001+ additive/idempotente"
      status: resolved
      verification: test — audit tests/migrations-non-destructive.test.ts (créé au plan 01-02) scanne src/core/database/migrations une fois ce plan livré ; convention docs/05 §7 (IF NOT EXISTS / INSERT OR IGNORE)
    - statement: "MUST NOT contenir de statement SQL (SELECT/INSERT/UPDATE/DELETE/CREATE) hors du module repositories (ni de la couche migrations, docs/05 §6-§7), et MUST NOT utiliser OFFSET dans toute requête de pagination"
      status: resolved
      verification: test — audit tests/sql-isolation.test.ts (créé au plan 01-02) ; client.ts (PRAGMAs) et m001.ts (DDL) sont dans la zone exemptée src/core/database/** sanctionnée par docs/05 §6/§7 — les schémas Zod de ce plan n'écrivent aucun SQL
---

<objective>
Poser la colonne vertébrale de données : le client SQLite avec sa séquence PRAGMAs, le migration runner schema_version + m001 (docs/05 §6 amendé 05-addendum-01), et les modules de schémas Zod feuilles (D-042, un fichier par table) avec les tests de caps aux limites — le tout prouvé sur le harness better-sqlite3 créé au plan 01-02.

Purpose: SPEC R2 + la moitié schémas de R3 — tout le reste de la phase (repositories 01-04, stores 01-05, boot 01-07) se branche sur ces contrats ; un défaut ici se propage partout.
Output: client + runner + m001, 10 fichiers de schémas feuilles, tests idempotence + boundaries verts (les audits 01-02 restent verts).
</objective>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
docs/05-data-model.md (§6 DDL lignes 155-350 — PRAGMAs 157-160, DDL 162-347 ; §7 Migrations lignes 351-409 ; §8 Schémas Zod miroirs lignes 423-732 ; §11 settings/backup lignes 1955-2036)
docs/05-addendum-01.md (intègre m001 directement : CHECK note_text assoupli, enums verrouillés)
tests/harness/sqliteHarness.ts (créé au plan 01-02 — surface DatabaseLike + openHarnessDb à consommer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client SQLite + migration runner + m001 (docs/05 §6 amendé)</name>
  <files>src/core/database/client.ts, src/core/database/migrations/runner.ts, src/core/database/migrations/m001.ts, tests/migrations/idempotency.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 155-350 (PRAGMAs 157-160 + DDL complet 162-347 — copier verbatim) et lignes 351-380 (conventions migrations : 1 transaction, version check, IF NOT EXISTS / INSERT OR IGNORE, non destructif)
    - docs/05-addendum-01.md (section 1 : le CHECK d'exclusion note_text/type='note' du doc 05 ligne 203 est SUPPRIMÉ de m001 — l'addendum prime ; le CHECK de longueur ligne 198 est conservé)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Code Examples "Client open + PRAGMAs" — noms d'API expo-sqlite SDK 57 verbatim)
    - tests/harness/sqliteHarness.ts (créé au plan 01-02 — le harness exécute CE runner, pas une copie)
  </read_first>
  <action>Créer src/core/database/client.ts : ouverture de babylog.db via openDatabaseAsync (SDK 57), puis la séquence PRAGMAs exacte de docs/05 §6 via execAsync dans l'ordre du doc (journal_mode WAL, foreign_keys ON, busy_timeout 5000, recursive_triggers OFF), export du handle typé et d'un alias de type DatabaseLike = SQLiteDatabase | Transaction (les méthodes des repositories prendront ce paramètre — les requêtes en transaction exclusive s'exécutent sur le handle txn, jamais sur le handle externe, Pitfall 1 RESEARCH). Créer src/core/database/migrations/m001.ts : le DDL de docs/05 §6 lignes 162-347 verbatim (7 tables : schema_version, baby_profile, log_event, timer_state, file_export, event, error_log ; 10 index ; 6 triggers updated_at ; INSERT OR IGNORE de la version 1), avec la seule modification autorisée par docs/05-addendum-01 : retirer le CHECK d'exclusion qui lie note_text au type note, en conservant le CHECK de longueur 2000 et tous les autres CHECK/enums tels quels (diaper_type wet/dirty/mixed/dry ; side left/right/both). Créer src/core/database/migrations/runner.ts : runMigrations(db) lit la version courante dans schema_version (table absente = version 0), applique chaque migration pendante en UNE transaction exclusive (requêtes sur le handle txn) dans l'ordre croissant, les PRAGMAs non transactionnels restant en amont (déjà posés par le client) ; re-exécuter sur une DB à jour ne change rien (idempotence par version + IF NOT EXISTS + INSERT OR IGNORE). Le runner tient un registre ordonné des migrations [m001] extensible m002+. Créer tests/migrations/idempotency.test.ts sur le harness de 01-02 (openHarnessDb importe ce runner réel — premier exercice de l'import) : première exécution crée 7 tables / 10 index / 6 triggers et une seule ligne schema_version=1 ; deuxième exécution = zéro changement (comptages identiques, version inchangée) ; le CHECK assoupli accepte une ligne note_text renseignée sur un événement de type feeding (preuve addendum). Après ce task, l'audit tests/migrations-non-destructive.test.ts de 01-02 bascule du mode fixtures au scan du DDL réel — il doit rester vert.</action>
  <verify>
    <automated>npx vitest run tests/migrations/idempotency.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou comptage tables/index/triggers différent de 7/10/6</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/migrations/idempotency.test.ts vert : 7 tables, 10 index, 6 triggers, exactement une ligne schema_version=1 après double exécution
    - m001.ts est le DDL docs/05 §6 avec l'unique déviation addendum (aucune autre retouche de CHECK, d'enum ou d'index)
    - Une insertion feeding avec note_text de 2000 caractères passe les CHECK (preuve addendum dans le test)
    - client.ts exporte le handle ouvert après la séquence PRAGMAs complète dans l'ordre docs/05
    - L'audit tests/migrations-non-destructive.test.ts (01-02) reste vert sur m001 réel
  </acceptance_criteria>
  <done>Client + runner + m001 en place, idempotence prouvée sur le harness, DDL conforme docs/05 amendé.</done>
</task>

<task type="auto">
  <name>Task 2: Schémas Zod feuilles — primitives + miroirs des 6 tables (D-042)</name>
  <files>src/db/schemas/primitives.ts, src/db/schemas/babyProfile.ts, src/db/schemas/logEvent.ts, src/db/schemas/timerState.ts, src/db/schemas/fileExport.ts, src/db/schemas/event.ts, src/db/schemas/errorLog.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 423-732 (schémas Zod miroirs : primitives uuid/epochMs/KeysetCursor + row/insert par table + superRefine cross-champs — le code doc est v3-styled mais tourne tel quel sur zod 4 ; écrire le NOUVEAU code en style v4 : z.strictObject, z.uuid(), conserver superRefine + z.ZodIssueCode.custom)
    - docs/05-addendum-01.md (le superRefine d'exclusion note/type disparaît des miroirs logEventRowSchema/logEventUpdateSchema en lockstep avec le DDL)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md (D-042 : modules feuilles n'important que zod ; direction D-041 préservée)
  </read_first>
  <action>Créer primitives.ts (uuidSchema z.uuid(), epochMsSchema entier non négatif, type KeysetCursor) puis les 6 miroirs de tables, un fichier par table (D-042) : babyProfile, logEvent, timerState, fileExport, event, errorLog — chacun avec le miroir row + insert (+ update quand docs/05 le définit) reprenant champ par champ les contraintes du DDL : stricts, caps exacts (name 1-80, message 1-1000, code <= 120, stack <= 20000, context <= 10000, properties <= 10000, file_name <= 255, duration_ms 0-86400000, amount_ml 0-5000, note_text <= 2000), enums lockstep addendum (diaper_type wet/dirty/mixed/dry ; side left/right/both), règles cross-champs via superRefine miroir des CHECK (sans l'exclusion note/type supprimée par l'addendum) ; exports z.infer typés. Chaque module n'importe que zod (et primitives siblings) — aucun import db/client/repositories (D-042, direction D-041 préservée), aucun SQL (zone d'isolation sanctionnée par l'audit 01-02).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit (miroirs mal typés ou exports z.infer invalides)</fails_when>
    <automated>test "$(ls src/db/schemas/*.ts 2>/dev/null | wc -l)" -ge 7 && ! grep -rL "from 'zod'" src/db/schemas --include='*.ts' | grep -qv primitives</automated>
    <fails_when>exit non-zero (moins de 7 modules présents, ou un module autre que primitives.ts n'importe pas zod — fichier probablement mal placé)</fails_when>
  </verify>
  <acceptance_criteria>
    - primitives.ts exporte uuidSchema (z.uuid()), epochMsSchema et le type KeysetCursor
    - Les 6 tables possèdent row + insert stricts avec exports z.infer (+ update là où docs/05 le définit), caps identiques au DDL
    - Le superRefine d'exclusion note/type est absent des miroirs logEvent (lockstep addendum)
    - Les 7 modules créés n'importent que zod (D-042 — gate grep vert sur les 7 fichiers)
    - npx tsc --noEmit vert
  </acceptance_criteria>
  <done>Primitives + 6 miroirs de tables stricts en place, zod uniquement — prêts pour backup/analytics/settings et les tests de caps.</done>
</task>

<task type="auto">
  <name>Task 3: Schémas backup/analytics/settings + tests de caps aux limites</name>
  <files>src/db/schemas/backup.ts, src/db/schemas/analytics.ts, src/db/schemas/settings.ts, tests/schemas/boundaries.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 1906-2036 (format backup v1 + backupSettingsSchema lignes 1963-1965 + clés settings:*/trial:* lignes 2019-2025) et lignes 1931-1933 (défauts night_mode auto / 1200 / 420) et lignes 2038-2124 (allowlist analytics)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md (D-042 : modules feuilles n'important que zod ; D-007 : contrat settings complet settings:* dès la Phase 1, trial:* non seedée)
    - tests/harness/sqliteHarness.ts (01-02 — insertions/lectures du test de caps via openHarnessDb)
    - src/db/schemas/primitives.ts + les 6 miroirs créés en Task 2 (les tests de caps importent les schémas feuilles)
  </read_first>
  <action>Créer les 3 modules complémentaires, chacun n'important que zod (et primitives siblings) — D-042 : backup.ts — schémas du format backup v1 du §10 dont le schéma settings de backup (nuit optionnelle : enum off/on/auto + minutes 0-1439) ; analytics.ts — allowlist des noms d'événements du §11 (dont night_mode_used) comme enum + schéma properties JSON ; settings.ts — contrat D-007 : constantes des trois clés settings:* (nuit + start/end minutes), valeurs par défaut docs/05 (auto, 1200, 420), schéma de valeurs, liste des clés trial:* documentée Phase 4 mais NON seedée en Phase 1. Créer tests/schemas/boundaries.test.ts : caps Zod note_text 2000 accepté / 2001 rejeté et amount_ml 5000 accepté / 5001 rejeté ; enums diaper_type et side verrouillés (rejet de toute autre valeur) ; passage Zod puis insertion via le harness de 01-02 pour prouver l'alignement Zod/CHECK dans les deux sens aux bornes ; round-trip UTF-8/emoji : une note de 2000 caractères mêlant emoji, accents français et caractères multi-octets insérée puis relue via le harness est identique (AC #15 moitié SQLite). À l'issue de ce task, le set complet des 10 modules feuilles existe et passe le gate grep D-042.</action>
  <verify>
    <automated>npx vitest run tests/schemas/boundaries.test.ts</automated>
    <fails_when>non-zero exit, tout cap en échec, ou "0 passed" dans le résumé</fails_when>
    <automated>test "$(ls src/db/schemas/*.ts 2>/dev/null | wc -l)" -ge 10 && ! grep -rL "from 'zod'" src/db/schemas --include='*.ts' | grep -qv primitives</automated>
    <fails_when>exit non-zero (moins de 10 modules présents, ou un module feuille autre que primitives.ts n'importe pas zod — fichier probablement mal placé)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/schemas/boundaries.test.ts vert : 2000/2001 note, 5000/5001 amount, enums diaper/side fermés, alignement Zod/CHECK aux deux bornes
    - Round-trip emoji/accents 2000 caractères dans note_text identique via le harness
    - Les 10 modules de src/db/schemas/ n'importent que zod (D-042 vérifié par grep — le gate assert d'abord la présence des 10 fichiers, puis l'absence d'import manquant)
    - backup.ts porte le format v1 §10 avec settings de backup nuit optionnelle ; analytics.ts porte l'allowlist §11 (dont night_mode_used)
    - settings.ts porte les 3 clés settings:* avec défauts auto/1200/420 et n'expose aucune clé trial seedée
  </acceptance_criteria>
  <done>Schémas miroirs complets et testés aux limites — prêts pour les 6 repositories (01-04) et le settings store (01-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entrée applicative → SQLite | Toute écriture futur utilisateur traverse Zod avant SQL (V5) |
| migrations → schéma device | Le DDL s'exécute sur l'appareil au premier boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03a | Tampering | Écritures hors validation Zod | high | mitigate | Schémas stricts aux caps exacts du DDL + tests de bornes 2000/2001 et 5000/5001 (les CHECK SQLite servent de second rempart) |
| T-01-03b | Denial of Service | Runner non idempotent au boot | medium | mitigate | Test d'idempotence (double exécution = no-op) + IF NOT EXISTS / INSERT OR IGNORE partout |
</threat_model>

<verification>
- npx vitest run tests/migrations/idempotency.test.ts tests/schemas/boundaries.test.ts vert
- Les trois audits du plan 01-02 restent verts (m001 réel scanné, client/m001 dans la zone sanctionnée)
- m001 conforme docs/05 §6 + addendum (7/10/6, CHECK assoupli, enums verrouillés)
- Gate grep D-042 vert (modules feuilles : zod uniquement)
</verification>

<success_criteria>
La colonne vertébrale de données existe et est prouvée : m001 idempotente conforme au doc amendé, client aux PRAGMAs docs/05, schémas Zod feuilles testés aux caps — le tout sous les audits fail-first du plan 01-02 qui restent verts.
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md` quand done
</output>
