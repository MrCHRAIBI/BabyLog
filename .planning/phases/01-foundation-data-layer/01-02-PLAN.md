---
phase: 01-foundation-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - vitest.config.ts
  - tests/harness/sqliteHarness.ts
  - tests/sql-isolation.test.ts
  - tests/migrations-non-destructive.test.ts
  - tests/zero-cloud-dependencies.test.ts
  - tests/__fixtures__/sql-isolation/violation.ts
  - tests/__fixtures__/sql-isolation/clean.ts
  - tests/__fixtures__/migrations/violation-migration.ts
  - tests/__fixtures__/pkg-with-cloud-sdk.json
  - src/core/database/client.ts
  - src/core/database/migrations/runner.ts
  - src/core/database/migrations/m001.ts
  - tests/migrations/idempotency.test.ts
  - src/db/schemas/primitives.ts
  - src/db/schemas/babyProfile.ts
  - src/db/schemas/logEvent.ts
  - src/db/schemas/timerState.ts
  - src/db/schemas/fileExport.ts
  - src/db/schemas/event.ts
  - src/db/schemas/errorLog.ts
  - src/db/schemas/backup.ts
  - src/db/schemas/analytics.ts
  - src/db/schemas/settings.ts
  - tests/schemas/boundaries.test.ts
autonomous: true
requirements: [OPS-02]
estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Re-exécuter le runner sur une DB déjà migrée n'applique aucun changement et laisse exactement une ligne schema_version=1 (SPEC R2, AC #2)"
    - "Le DDL m001 crée exactement 7 tables, 10 index, 6 triggers, avec le CHECK note_text assoupli par 05-addendum-01 (longueur <= 2000 sur tout type, sans exclusion de type) et les enums diaper_type/side verrouillés"
    - "Caps Zod aux limites : note_text 2000 caractères acceptés / 2001 rejetés ; amount_ml 5000 accepté / 5001 rejeté — côté Zod ET côté CHECK SQLite via le harness (SPEC R3, AC #15)"
    - "Round-trip UTF-8/emoji dans note_text : une note de 2000 caractères contenant emoji + accents insérée puis relue via le harness est identique octet pour octet (AC #15, moitié SQLite)"
    - "Le harness rejoue la séquence PRAGMAs de docs/05 §6 (foreign_keys=ON, busy_timeout=5000, recursive_triggers=OFF) — aucun test ne passe en s'appuyant sur un défaut de driver (better-sqlite3 FK ON par défaut ≠ expo-sqlite OFF)"
    - "La suite d'audit est en place et fail-first prouvé : chaque test passe sur l'état propre et détecte sa violation fixture"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Runner vitest + resolve.alias '@' → ./src (vitest ne lit pas tsconfig paths) + include tests/**"
    - path: "tests/harness/sqliteHarness.ts"
      provides: "Adaptateur better-sqlite3 :memory: exposant la surface expo-sqlite (getAllAsync, getFirstAsync, runAsync, execAsync, withExclusiveTransactionAsync) + type DatabaseLike"
    - path: "tests/sql-isolation.test.ts"
      provides: "Audit SQL : mots-clés SQL interdits hors src/db/repositories et src/core/database ; mot-clé de pagination banni interdit partout"
    - path: "tests/migrations-non-destructive.test.ts"
      provides: "Audit migrations : aucun DROP/DELETE de données/ALTER-drop dans src/core/database/migrations"
    - path: "tests/zero-cloud-dependencies.test.ts"
      provides: "Audit allowlist/denylist de package.json — zéro SDK cloud/analytics/crash"
    - path: "src/core/database/client.ts"
      provides: "openDatabaseAsync('babylog.db') + séquence PRAGMAs execAsync + type DatabaseLike partagé"
    - path: "src/core/database/migrations/runner.ts"
      provides: "runMigrations(db) — lecture schema_version, application en 1 transaction par migration, idempotent"
    - path: "src/core/database/migrations/m001.ts"
      provides: "DDL docs/05 §6 (162-347) verbatim amendé addendum — 7 tables / 10 index / 6 triggers"
    - path: "src/db/schemas/"
      provides: "9 modules feuilles Zod (D-042) : primitives + 6 tables + backup/analytics/settings, imports zod uniquement"
    - path: "tests/migrations/idempotency.test.ts"
      provides: "Preuve idempotence runner + comptage tables/index/triggers"
    - path: "tests/schemas/boundaries.test.ts"
      provides: "Caps Zod + CHECK (note 2000/2001, amount 5000/5001, enums) + round-trip emoji note_text"
  key_links:
    - from: "tests/harness/sqliteHarness.ts"
      to: "src/core/database/migrations/m001.ts"
      via: "le harness exécute le DDL m001 réel (mêmes SQL, mêmes PRAGMAs rejoués)"
      pattern: "runMigrations|M001"
    - from: "src/db/schemas/logEvent.ts"
      to: "tests/schemas/boundaries.test.ts"
      via: "les caps testés importent les schémas feuilles (D-042 : zod uniquement)"
      pattern: "from '@/db/schemas"
  prohibitions:
    - statement: "MUST NOT inclure tout SDK cloud/analytics/crash-reporting/tierce-party (promesse zéro-cloud D-024/OPS)"
      status: resolved
      verification: test — tests/zero-cloud-dependencies.test.ts scanne package.json contre la denylist cloud/analytics/crash + interdits D-024 ; violation fixture tests/__fixtures__/pkg-with-cloud-sdk.json, clean fixture package.json racine
    - statement: "MUST NOT contenir de SQL destructif (DROP/DELETE de données, ALTER drop) dans toute migration publiée — m001+ additive/idempotente"
      status: resolved
      verification: test — tests/migrations-non-destructive.test.ts scanne src/core/database/migrations ; violation fixture tests/__fixtures__/migrations/violation-migration.ts, clean fixture m001 (style IF NOT EXISTS)
    - statement: "MUST NOT contenir de statement SQL (SELECT/INSERT/UPDATE/DELETE/CREATE) hors du module repositories (ni de la couche migrations, docs/05 §6-§7), et MUST NOT utiliser OFFSET dans toute requête de pagination"
      status: resolved
      verification: test — tests/sql-isolation.test.ts scanne src/ hors src/db/repositories + src/core/database pour les mots-clés SQL, et src/ entier pour OFFSET ; violation fixture tests/__fixtures__/sql-isolation/violation.ts, clean fixture tests/__fixtures__/sql-isolation/clean.ts
---

<objective>
Poser la colonne vertébrale de données et le Wave 0 de validation : harness Vitest in-memory SQLite (better-sqlite3) rejouant les PRAGMAs app, la suite d'audits fail-first (isolation SQL + pagination, migrations non destructives, zero-cloud), le client SQLite avec sa séquence PRAGMAs, le migration runner schema_version + m001 (docs/05 §6 amendé 05-addendum-01), et les 9 modules de schémas Zod feuilles (D-042) avec les tests de caps aux limites.

Purpose: SPEC R2 + la moitié schémas de R3 + les prohibitions R2/R3/R10 — tout le reste de la phase (repositories 01-03, boot 01-06) se branche sur ces contrats ; un défaut ici se propage partout.
Output: harness + 3 audits + fixtures, client + runner + m001, 9 fichiers de schémas, tests idempotence + boundaries verts.
</objective>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
docs/05-data-model.md (§6 DDL lignes 155-350 — PRAGMAs 157-160, DDL 162-347 ; §7 Migrations lignes 351-409 ; §8 Schémas Zod miroirs lignes 423-732 ; §11 settings/backup lignes 1955-2036)
docs/05-addendum-01.md (intègre m001 directement : CHECK note_text assoupli, enums verrouillés)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — vitest.config + harness better-sqlite3 + suite d'audits fail-first</name>
  <files>vitest.config.ts, tests/harness/sqliteHarness.ts, tests/sql-isolation.test.ts, tests/migrations-non-destructive.test.ts, tests/zero-cloud-dependencies.test.ts, tests/__fixtures__/sql-isolation/violation.ts, tests/__fixtures__/sql-isolation/clean.ts, tests/__fixtures__/migrations/violation-migration.ts, tests/__fixtures__/pkg-with-cloud-sdk.json</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 1 harness verbatim + Validation Architecture / Wave 0 Gaps + parité drivers : FK ON par défaut better-sqlite3, WAL inapplicable en :memory:)
    - docs/05-data-model.md lignes 155-160 (séquence PRAGMAs exacte à rejouer) et lignes 413-421 (règle commune repositories — objet de l'audit d'isolation)
    - tsconfig.json (alias @/ à miroir dans vitest — vitest ne lit PAS tsconfig paths)
    - .planning/phases/01-foundation-data-layer/01-SPEC.md (section Prohibitions — descripteurs de check)
  </read_first>
  <action>Créer vitest.config.ts : environnement node, include tests/**/*.test.ts, resolve.alias mappant @/ vers ./src (le miroir de tsconfig — jamais compter sur tsconfig paths). Créer tests/harness/sqliteHarness.ts selon RESEARCH Pattern 1 : better-sqlite3 sur :memory:, replay de la séquence PRAGMAs de docs/05 (foreign_keys ON, busy_timeout 5000, recursive_triggers OFF — journal_mode volontairement omis, inapplicable en mémoire), exposition async de la surface expo-sqlite utilisée par les repositories (getAllAsync, getFirstAsync, runAsync, execAsync) et mapping de withExclusiveTransactionAsync sur BEGIN IMMEDIATE / COMMIT / ROLLBACK avec exécution sur le handle de transaction ; exporter un type DatabaseLike union (handle DB | handle txn) et une fonction openHarnessDb() qui applique m001 via le runner réel (import depuis src/core/database — le harness teste le code de prod, pas une copie). Créer les trois audits, chacun avec paire violation/clean prouvant le fail-first : (1) tests/sql-isolation.test.ts scanne tous les .ts/.tsx sous src/ SAUF src/db/repositories/** et src/core/database/** (le DDL des migrations et les PRAGMAs du client y sont sanctionnés par docs/05 §6/§7) contre les mots-clés SQL (SELECT, INSERT, UPDATE, DELETE, CREATE TABLE/INDEX/TRIGGER, PRAGMA) insensible à la casse, et scanne src/ EN ENTIER (repositories inclus) contre le mot-clé de pagination saut-de-page interdit par docs/05 §8 ; (2) tests/migrations-non-destructive.test.ts scanne src/core/database/migrations/** contre DROP TABLE, DROP INDEX, DELETE FROM et ALTER TABLE ... DROP — avec garde : si le dossier migrations est vide avant la Task 2, le test s'exécute en mode fixtures uniquement ; (3) tests/zero-cloud-dependencies.test.ts lit package.json (dependencies + devDependencies) et échoue si un nom correspond aux denylist cloud/analytics/crash (firebase, sentry, amplitude, segment, mixpanel, datadog, bugsnag, appcenter, onesignal et similaires) ou aux deux paquets interdits D-024. Fixtures : violation SQL dans un hook factice + clean repositories-only ; violation migration avec une table droppée + miroir clean au style IF NOT EXISTS de m001 ; fixture package.json cloud (tests/__fixtures__/pkg-with-cloud-sdk.json) avec le package.json racine comme clean. Chaque audit doit, sur sa violation, rapporter fichier + ligne et échouer, et passer sur son clean.</action>
  <verify>
    <automated>npx vitest run tests/sql-isolation.test.ts tests/migrations-non-destructive.test.ts tests/zero-cloud-dependencies.test.ts</automated>
    <fails_when>non-zero exit, un test en échec, ou "0 passed" dans le résumé (les trois audits + leurs modes fixtures doivent tourner)</fails_when>
  </verify>
  <acceptance_criteria>
    - npx vitest run sur les trois fichiers d'audit : tous verts
    - L'audit SQL rapporte la violation fixture (fichier + ligne du hook factice) et passe sur la fixture clean repositories-only
    - L'audit migrations rapporte la violation fixture (DROP TABLE) et accepte le miroir clean IF NOT EXISTS
    - L'audit zero-cloud échoue sur tests/__fixtures__/pkg-with-cloud-sdk.json (SDK cloud présent) et passe sur le package.json racine
    - sqliteHarness.ts applique les PRAGMAs docs/05 à l'ouverture et n'invoque jamais journal_mode en mémoire
  </acceptance_criteria>
  <done>Wave 0 complet : harness opérationnel, trois audits verts sur l'état propre et rouges sur leurs fixtures fail-first.</done>
</task>

<task type="auto">
  <name>Task 2: Client SQLite + migration runner + m001 (docs/05 §6 amendé)</name>
  <files>src/core/database/client.ts, src/core/database/migrations/runner.ts, src/core/database/migrations/m001.ts, tests/migrations/idempotency.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 155-350 (PRAGMAs 157-160 + DDL complet 162-347 — copier verbatim) et lignes 351-380 (conventions migrations : 1 transaction, version check, IF NOT EXISTS / INSERT OR IGNORE, non destructif)
    - docs/05-addendum-01.md (section 1 : le CHECK d'exclusion note_text/type='note' du doc 05 ligne 203 est SUPPRIMÉ de m001 — l'addendum prime ; le CHECK de longueur ligne 198 est conservé)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Code Examples "Client open + PRAGMAs" — noms d'API expo-sqlite SDK 57 verbatim)
    - tests/harness/sqliteHarness.ts (créé en Task 1 — le harness exécute CE runner, pas une copie)
  </read_first>
  <action>Créer src/core/database/client.ts : ouverture de babylog.db via openDatabaseAsync (SDK 57), puis la séquence PRAGMAs exacte de docs/05 §6 via execAsync dans l'ordre du doc (journal_mode WAL, foreign_keys ON, busy_timeout 5000, recursive_triggers OFF), export du handle typé et d'un alias de type DatabaseLike = SQLiteDatabase | Transaction (les méthodes des repositories prendront ce paramètre — les requêtes en transaction exclusive s'exécutent sur le handle txn, jamais sur le handle externe, Pitfall 1 RESEARCH). Créer src/core/database/migrations/m001.ts : le DDL de docs/05 §6 lignes 162-347 verbatim (7 tables : schema_version, baby_profile, log_event, timer_state, file_export, event, error_log ; 10 index ; 6 triggers updated_at ; INSERT OR IGNORE de la version 1), avec la seule modification autorisée par docs/05-addendum-01 : retirer le CHECK d'exclusion qui lie note_text au type note, en conservant le CHECK de longueur 2000 et tous les autres CHECK/enums tels quels (diaper_type wet/dirty/mixed/dry ; side left/right/both). Créer src/core/database/migrations/runner.ts : runMigrations(db) lit la version courante dans schema_version (table absente = version 0), applique chaque migration pendante en UNE transaction exclusive (requêtes sur le handle txn) dans l'ordre croissant, les PRAGMAs non transactionnels restant en amont (déjà posés par le client) ; re-exécuter sur une DB à jour ne change rien (idempotence par version + IF NOT EXISTS + INSERT OR IGNORE). Le runner tient un registre ordonné des migrations [m001] extensible m002+. Créer tests/migrations/idempotency.test.ts sur le harness : première exécution crée 7 tables / 10 index / 6 triggers et une seule ligne schema_version=1 ; deuxième exécution = zéro changement (comptages identiques, version inchangée) ; le CHECK assoupli accepte une ligne note_text renseignée sur un événement de type feeding (preuve addendum).</action>
  <verify>
    <automated>npx vitest run tests/migrations/idempotency.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou comptage tables/index/triggers différent de 7/10/6</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/migrations/idempotency.test.ts vert : 7 tables, 10 index, 6 triggers, exactement une ligne schema_version=1 après double exécution
    - m001.ts est le DDL docs/05 §6 avec l'unique déviation addendum (aucune autre retouche de CHECK, d'enum ou d'index)
    - Une insertion feeding avec note_text de 2000 caractères passe les CHECK (preuve addendum dans le test)
    - client.ts exporte le handle ouvert après la séquence PRAGMAs complète dans l'ordre docs/05
    - L'audit tests/migrations-non-destructive.test.ts reste vert sur m001 réel
  </acceptance_criteria>
  <done>Client + runner + m001 en place, idempotence prouvée sur le harness, DDL conforme docs/05 amendé.</done>
</task>

<task type="auto">
  <name>Task 3: Schémas Zod feuilles (D-042) + tests de caps aux limites</name>
  <files>src/db/schemas/primitives.ts, src/db/schemas/babyProfile.ts, src/db/schemas/logEvent.ts, src/db/schemas/timerState.ts, src/db/schemas/fileExport.ts, src/db/schemas/event.ts, src/db/schemas/errorLog.ts, src/db/schemas/backup.ts, src/db/schemas/analytics.ts, src/db/schemas/settings.ts, tests/schemas/boundaries.test.ts</files>
  <read_first>
    - docs/05-data-model.md lignes 423-732 (schémas Zod miroirs : primitives uuid/epochMs/KeysetCursor + row/insert par table + superRefine cross-champs — le code doc est v3-styled mais tourne tel quel sur zod 4 ; écrire le NOUVEAU code en style v4 : z.strictObject, z.uuid(), conserver superRefine + z.ZodIssueCode.custom)
    - docs/05-data-model.md lignes 1906-2036 (format backup v1 + backupSettingsSchema lignes 1963-1965 + clés settings:*/trial:* lignes 2019-2025) et lignes 1931-1933 (défauts night_mode auto / 1200 / 420) et lignes 2038-2124 (allowlist analytics)
    - docs/05-addendum-01.md (le superRefine d'exclusion note/type disparaît des miroirs logEventRowSchema/logEventUpdateSchema en lockstep avec le DDL)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md (D-042 : modules feuilles n'important que zod ; D-007 : contrat settings)
  </read_first>
  <action>Créer les 9 modules feuilles sous src/db/schemas/ : primitives.ts (uuidSchema z.uuid(), epochMsSchema entier non négatif, type KeysetCursor) ; un fichier par table (babyProfile, logEvent, timerState, fileExport, event, errorLog) avec le miroir row + insert (+ update quand docs/05 le définit) reprenant champ par champ les contraintes du DDL : stricts, caps exacts (name 1-80, message 1-1000, code <= 120, stack <= 20000, context <= 10000, properties <= 10000, file_name <= 255, duration_ms 0-86400000, amount_ml 0-5000, note_text <= 2000), enums lockstep addendum, règles cross-champs via superRefine miroir des CHECK (sans l'exclusion note/type supprimée) ; exports z.infer typés. Chaque module n'importe que zod (et primitives siblings) — aucun import db/client/repositories (D-042, direction D-041). backup.ts : schémas du format backup v1 du §10 dont le schéma settings de backup (nuit optionnelle : enum off/on/auto + minutes 0-1439). analytics.ts : allowlist des noms d'événements du §11 (dont night_mode_used) comme enum + schéma properties JSON. settings.ts : contrat D-007 — constantes des trois clés settings:* (nuit + start/end minutes), valeurs par défaut docs/05 (auto, 1200, 420), schéma de valeurs, liste des clés trial:* documentée Phase 4 mais NON seedée en Phase 1. Créer tests/schemas/boundaries.test.ts : caps Zod note_text 2000 accepté / 2001 rejeté et amount_ml 5000 accepté / 5001 rejeté ; enums diaper_type et side verrouillés (rejet de toute autre valeur) ; passage Zod puis insertion via le harness pour prouver l'alignement Zod/CHECK dans les deux sens aux bornes ; round-trip UTF-8/emoji : une note de 2000 caractères mêlant emoji, accents français et caractères multi-octets insérée puis relue via le harness est identique (AC #15 moitié SQLite).</action>
  <verify>
    <automated>npx vitest run tests/schemas/boundaries.test.ts</automated>
    <fails_when>non-zero exit, tout cap en échec, ou "0 passed" dans le résumé</fails_when>
    <automated>grep -rL "from 'zod'" src/db/schemas --include='*.ts' | grep -v primitives || echo LEAF_OK</automated>
    <fails_when>sortie autre que LEAF_OK (un module feuille n'importe pas zod → fichier probablement mal placé)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/schemas/boundaries.test.ts vert : 2000/2001 note, 5000/5001 amount, enums diaper/side fermés, alignement Zod/CHECK aux deux bornes
    - Round-trip emoji/accents 2000 caractères dans note_text identique via le harness
    - Les 9 modules de src/db/schemas/ n'importent que zod (D-042 vérifié par grep)
    - Chaque table possède row + insert stricts avec exports z.infer, caps identiques au DDL
    - settings.ts porte les 3 clés settings:* avec défauts auto/1200/420 et n'expose aucune clé trial seedée
  </acceptance_criteria>
  <done>Schémas miroirs complets et testés aux limites — prêts pour les 6 repositories (01-03) et le settings store (01-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entrée applicative → SQLite | Toute écriture futur utilisateur traverse Zod avant SQL (V5) |
| package.json → bundle | Surface supply-chain auditée en 01-01, surveillée ici par test |
| migrations → schéma device | Le DDL s'exécute sur l'appareil au premier boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02a | Tampering | Écritures hors validation Zod | high | mitigate | Schémas stricts aux caps exacts du DDL + tests de bornes 2000/2001 et 5000/5001 (les CHECK SQLite servent de second rempart) |
| T-01-02b | Tampering | SQL injecté hors repositories | high | mitigate | Audit sql-isolation fail-first : mots-clés SQL confinés aux repositories + couche migrations, pagination keyset-only vérifiée partout |
| T-01-02c | Tampering | Migration destructive publiée | high | mitigate | Audit migrations-non-destructive fail-first + convention docs/05 §7 (jamais modifier une migration publiée) |
| T-01-02d | Information Disclosure | Dépendance cloud exfiltrant des données | high | mitigate | Audit zero-cloud fail-first sur package.json + fixtures violation/clean |
| T-01-02e | Denial of Service | Runner non idempotent au boot | medium | mitigate | Test d'idempotence (double exécution = no-op) + IF NOT EXISTS / INSERT OR IGNORE partout |
</threat_model>

<verification>
- npx vitest run (suite complète) vert : audits + idempotence + boundaries
- Fail-first prouvé par les trois paires de fixtures
- m001 conforme docs/05 §6 + addendum (7/10/6, CHECK assoupli, enums verrouillés)
</verification>

<success_criteria>
La colonne vertébrale de données existe et est prouvée : harness fidèle aux PRAGMAs app, m001 idempotente conforme au doc amendé, schémas Zod feuilles testés aux caps — les trois prohibitions testables de la phase sont verrouillées par des audits fail-first.
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md` quand done
</output>
