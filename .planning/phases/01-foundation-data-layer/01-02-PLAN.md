---
phase: 01-foundation-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - vitest.config.ts
  - tests/harness/sqliteHarness.ts
  - tests/sql-isolation.test.ts
  - tests/migrations-non-destructive.test.ts
  - tests/zero-cloud-dependencies.test.ts
  - tests/__fixtures__/sql-isolation/violation.ts
  - tests/__fixtures__/sql-isolation/clean.ts
  - tests/__fixtures__/migrations/violation-migration.ts
  - tests/__fixtures__/pkg-with-cloud-sdk.json
autonomous: true
requirements: [OPS-02]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Le harness rejoue la séquence PRAGMAs de docs/05 §6 (foreign_keys=ON, busy_timeout=5000, recursive_triggers=OFF) — aucun test de la phase ne passe en s'appuyant sur un défaut de driver (better-sqlite3 FK ON par défaut ≠ expo-sqlite OFF) ; sa vérification fonctionnelle complète (exécution du DDL via le runner réel) arrive avec le plan 01-03"
    - "La suite d'audit est en place et fail-first prouvé : chaque test passe sur l'état propre et détecte sa violation fixture"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Runner vitest + resolve.alias '@' → ./src (vitest ne lit pas tsconfig paths) + include tests/**"
    - path: "tests/harness/sqliteHarness.ts"
      provides: "Adaptateur better-sqlite3 :memory: exposant la surface expo-sqlite (getAllAsync, getFirstAsync, runAsync, execAsync, withExclusiveTransactionAsync) + type DatabaseLike + openHarnessDb() appliquant m001 via le runner RÉEL de 01-03 (le harness teste le code de prod, pas une copie)"
    - path: "tests/sql-isolation.test.ts"
      provides: "Audit SQL : mots-clés SQL interdits hors src/db/repositories et src/core/database ; mot-clé de pagination banni interdit partout"
    - path: "tests/migrations-non-destructive.test.ts"
      provides: "Audit migrations : aucun DROP/DELETE de données/ALTER-drop dans src/core/database/migrations ; mode fixtures-only tant que le dossier migrations est vide (créé au plan 01-03)"
    - path: "tests/zero-cloud-dependencies.test.ts"
      provides: "Audit allowlist/denylist de package.json — zéro SDK cloud/analytics/crash"
  key_links:
    - from: "tests/harness/sqliteHarness.ts"
      to: "src/core/database/migrations/m001.ts (plan 01-03)"
      via: "openHarnessDb() applique le DDL m001 réel via le runner réel (mêmes SQL, mêmes PRAGMAs rejoués) — import exercé dès le premier test du plan 01-03"
      pattern: "runMigrations|M001"
  prohibitions:
    - statement: "MUST NOT inclure tout SDK cloud/analytics/crash-reporting/tierce-party (promesse zéro-cloud D-024/OPS)"
      status: resolved
      verification: test — tests/zero-cloud-dependencies.test.ts scanne package.json contre la denylist cloud/analytics/crash + interdits D-024 ; violation fixture tests/__fixtures__/pkg-with-cloud-sdk.json, clean fixture package.json racine
    - statement: "MUST NOT contenir de SQL destructif (DROP/DELETE de données, ALTER drop) dans toute migration publiée — m001+ additive/idempotente"
      status: resolved
      verification: test — tests/migrations-non-destructive.test.ts scanne src/core/database/migrations (créé au plan 01-03) ; violation fixture tests/__fixtures__/migrations/violation-migration.ts, clean fixture m001 (style IF NOT EXISTS)
    - statement: "MUST NOT contenir de statement SQL (SELECT/INSERT/UPDATE/DELETE/CREATE) hors du module repositories (ni de la couche migrations, docs/05 §6-§7), et MUST NOT utiliser OFFSET dans toute requête de pagination"
      status: resolved
      verification: test — tests/sql-isolation.test.ts scanne src/ hors src/db/repositories + src/core/database pour les mots-clés SQL, et src/ entier pour le mot-clé de pagination banni ; l'exemption src/core/database/** est documentée et sanctionnée (le DDL des migrations et les PRAGMAs du client y sont mandatés par docs/05 §6/§7 — SPEC R2) — déviation de périmètre consciente, à rappeler dans le summary de phase ; violation fixture tests/__fixtures__/sql-isolation/violation.ts, clean fixture tests/__fixtures__/sql-isolation/clean.ts
---

<objective>
Poser le Wave 0 de validation : config Vitest avec l'alias @, le harness in-memory SQLite (better-sqlite3) rejouant les PRAGMAs app et exposant la surface expo-sqlite, et la suite d'audits fail-first (isolation SQL + pagination, migrations non destructives, zero-cloud) avec leurs paires de fixtures violation/clean.

Purpose: les trois prohibitions testables de la phase (SPEC R2/R3/R10) sont verrouillées ici par des audits qui échouent sur leur violation — tout le reste de la phase (DB spine 01-03, repositories 01-04, boot 01-07) construit sous ces gardes-fous ; un défaut dans le harness se propagerait à tous les tests.
Output: vitest.config.ts, harness better-sqlite3, 3 audits + 4 fixtures, tous verts sur l'état propre et rouges sur leurs violations.
</objective>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
docs/05-data-model.md (§6 PRAGMAs lignes 155-160 à rejouer ; lignes 413-421 règle commune repositories — objet de l'audit d'isolation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: vitest.config + audits sql-isolation et zero-cloud fail-first</name>
  <files>vitest.config.ts, tests/sql-isolation.test.ts, tests/zero-cloud-dependencies.test.ts, tests/__fixtures__/sql-isolation/violation.ts, tests/__fixtures__/sql-isolation/clean.ts, tests/__fixtures__/pkg-with-cloud-sdk.json</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Validation Architecture / Wave 0 Gaps + parité drivers : FK ON par défaut better-sqlite3, WAL inapplicable en :memory:)
    - docs/05-data-model.md lignes 413-421 (règle commune repositories — objet de l'audit d'isolation)
    - tsconfig.json (alias @/ à miroir dans vitest — vitest ne lit PAS tsconfig paths)
    - .planning/phases/01-foundation-data-layer/01-SPEC.md (section Prohibitions — descripteurs de check)
  </read_first>
  <action>Créer vitest.config.ts : environnement node, include tests/**/*.test.ts, resolve.alias mappant @/ vers ./src (le miroir de tsconfig — jamais compter sur tsconfig paths). Créer tests/sql-isolation.test.ts : scanne tous les .ts/.tsx sous src/ SAUF src/db/repositories/** et src/core/database/** (le DDL des migrations et les PRAGMAs du client y sont sanctionnés par docs/05 §6/§7 — exemption de périmètre documentée dans les prohibitions du frontmatter) contre les mots-clés SQL (SELECT, INSERT, UPDATE, DELETE, CREATE TABLE/INDEX/TRIGGER, PRAGMA) insensible à la casse, et scanne src/ EN ENTIER (repositories inclus) contre le mot-clé de pagination saut-de-page interdit par docs/05 §8. Créer tests/zero-cloud-dependencies.test.ts : lit package.json (dependencies + devDependencies) et échoue si un nom correspond aux denylist cloud/analytics/crash (firebase, sentry, amplitude, segment, mixpanel, datadog, bugsnag, appcenter, onesignal et similaires) ou aux deux paquets interdits D-024. Fixtures de fail-first : tests/__fixtures__/sql-isolation/violation.ts (statement SQL dans un hook factice — hors des répertoires sanctionnés) et clean.ts (repositories-only au style docs/05) ; tests/__fixtures__/pkg-with-cloud-sdk.json (package.json témoin avec un SDK cloud) avec le package.json racine comme clean. Chaque audit doit, sur sa violation, rapporter fichier + ligne et échouer, et passer sur son clean.</action>
  <verify>
    <automated>npx vitest run tests/sql-isolation.test.ts tests/zero-cloud-dependencies.test.ts</automated>
    <fails_when>non-zero exit, un test en échec, ou "0 passed" dans le résumé (les deux audits + leurs modes fixtures doivent tourner)</fails_when>
  </verify>
  <acceptance_criteria>
    - npx vitest run sur les deux audits : tous verts
    - L'audit SQL rapporte la violation fixture (fichier + ligne du hook factice) et passe sur la fixture clean repositories-only
    - L'audit zero-cloud échoue sur tests/__fixtures__/pkg-with-cloud-sdk.json (SDK cloud présent) et passe sur le package.json racine
    - vitest.config.ts résout l'alias @/ vers ./src
  </acceptance_criteria>
  <done>Config vitest + deux premiers audits fail-first opérationnels (isolation SQL + pagination, zero-cloud).</done>
</task>

<task type="auto">
  <name>Task 2: Harness better-sqlite3 (surface expo-sqlite, PRAGMAs rejoués) + audit migrations non destructives</name>
  <files>tests/harness/sqliteHarness.ts, tests/migrations-non-destructive.test.ts, tests/__fixtures__/migrations/violation-migration.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 1 harness verbatim — better-sqlite3 :memory:, replay PRAGMAs, mapping withExclusiveTransactionAsync)
    - docs/05-data-model.md lignes 155-160 (séquence PRAGMAs exacte à rejouer) et lignes 353-380 (conventions migrations — objet de l'audit)
    - .planning/phases/01-foundation-data-layer/01-SPEC.md (prohibition migrations non destructives)
  </read_first>
  <action>Créer tests/harness/sqliteHarness.ts selon RESEARCH Pattern 1 : better-sqlite3 sur :memory:, replay de la séquence PRAGMAs de docs/05 (foreign_keys ON, busy_timeout 5000, recursive_triggers OFF — journal_mode volontairement omis, inapplicable en mémoire), exposition async de la surface expo-sqlite utilisée par les repositories (getAllAsync, getFirstAsync, runAsync, execAsync) et mapping de withExclusiveTransactionAsync sur BEGIN IMMEDIATE / COMMIT / ROLLBACK avec exécution sur le handle de transaction ; exporter un type DatabaseLike union (handle DB | handle txn) et une fonction openHarnessDb() qui appliquera m001 via le runner RÉEL (import depuis src/core/database — le harness teste le code de prod, pas une copie ; cet import est exercé et vérifié dès le premier test du plan 01-03, aucun test de CE plan ne l'importe encore). Créer tests/migrations-non-destructive.test.ts : scanne src/core/database/migrations/** contre DROP TABLE, DROP INDEX, DELETE FROM et ALTER TABLE ... DROP — avec garde : le dossier migrations est vide jusqu'au plan 01-03, le test s'exécute alors en mode fixtures uniquement. Fixture tests/__fixtures__/migrations/violation-migration.ts : une migration témoin avec une table droppée + miroir clean au style IF NOT EXISTS de m001 (dans le même fichier fixture ou fichier voisin). L'audit doit, sur sa violation, rapporter fichier + ligne et échouer, et accepter le style IF NOT EXISTS.</action>
  <verify>
    <automated>npx vitest run tests/sql-isolation.test.ts tests/migrations-non-destructive.test.ts tests/zero-cloud-dependencies.test.ts</automated>
    <fails_when>non-zero exit, un test en échec, ou "0 passed" dans le résumé (les trois audits doivent tourner ensemble)</fails_when>
  </verify>
  <acceptance_criteria>
    - Les trois audits tournent ensemble et sont verts
    - L'audit migrations rapporte la violation fixture (DROP TABLE) et accepte le miroir clean IF NOT EXISTS
    - Le mode fixtures-only est actif tant que src/core/database/migrations est vide (le plan 01-03 le fera basculer sur le DDL réel)
    - sqliteHarness.ts applique les PRAGMAs docs/05 à l'ouverture et n'invoque jamais journal_mode en mémoire
  </acceptance_criteria>
  <done>Wave 0 complet : harness opérationnel (vérification fonctionnelle au plan 01-03), trois audits verts sur l'état propre et rouges sur leurs fixtures fail-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → bundle | Surface supply-chain auditée en 01-01, surveillée ici par test |
| arbre src/ → audit statique | Les audits scandent le source — faux négatif = prohibition ouverte |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02a | Tampering | SQL injecté hors repositories | high | mitigate | Audit sql-isolation fail-first : mots-clés SQL confinés aux repositories + couche migrations, pagination keyset-only vérifiée partout ; paires violation/clean prouvent la détection |
| T-01-02b | Tampering | Migration destructive publiée | high | mitigate | Audit migrations-non-destructive fail-first + convention docs/05 §7 (jamais modifier une migration publiée) ; mode fixtures prouvé avant l'arrivée du DDL 01-03 |
| T-01-02c | Information Disclosure | Dépendance cloud exfiltrant des données | high | mitigate | Audit zero-cloud fail-first sur package.json + fixtures violation/clean (promesse D-024/OPS) |
</threat_model>

<verification>
- npx vitest run (les trois audits) vert
- Fail-first prouvé par les trois paires de fixtures
- Harness : PRAGMAs docs/05 rejoués, surface expo-sqlite exposée (sa preuve fonctionnelle arrive avec 01-03)
</verification>

<success_criteria>
Le Wave 0 de validation existe et est prouvé : harness fidèle aux PRAGMAs app, trois audits fail-first verrouillant les prohibitions testables de la phase (isolation SQL + pagination, migrations non destructives, zéro cloud) — le reste de la phase construit sous ces gardes-fous.
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md` quand done
</output>
