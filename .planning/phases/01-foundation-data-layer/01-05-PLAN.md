---
phase: 01-foundation-data-layer
plan: 05
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/core/storage/mmkv.ts
  - src/core/storage/secureStorage.ts
  - src/core/storage/zustandAdapter.ts
  - src/stores/settingsStore.ts
  - src/stores/entitlementsStore.ts
  - src/i18n/index.ts
  - src/i18n/locales/en/common.json
  - src/i18n/locales/fr/common.json
  - tests/stores/settings-persist.test.ts
  - tests/entitlements-encrypted-instance.test.ts
  - tests/i18n/fallback.test.ts
autonomous: true
requirements: [OPS-02]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Un réglage modifié via le settings store survit à un kill/relaunch (relecture par instance fraîche) et une double écriture conserve la dernière valeur au relaunch (SPEC R4, AC #5)"
    - "Le store entitlements est construit UNIQUEMENT sur l'instance MMKV chiffrée — jamais l'instance standard, jamais SQLite, jamais en clair (SPEC R4, AC #6, prohibition testée)"
    - "La clé de chiffrement MMKV est générée (expo-crypto), stockée dans SecureStore, et une erreur de lecture SecureStore échoue DUREMENT — la clé n'est jamais régénérée silencieusement"
    - "Une clé absente du JSON FR rend la string EN — jamais la clé brute, jamais un crash (SPEC R7, AC #11)"
    - "i18next est initialisé depuis les JSON statiques bundlés (zéro fetch), avec fallbackLng en et la locale détectée via expo-localization"
    - "Round-trip UTF-8/emoji dans le JSON FR : une clé contenant emoji/accents rendue par i18n.t() est identique (AC #15, moitié i18n)"
    - "Le contrat settings:* complet de D-007 est seedé : night_mode off/on/auto (défaut auto), night_mode_start_minutes 1200, night_mode_end_minutes 420 — aucune clé trial:* seedée (Phase 4)"
    - statement: "Kill pendant une écriture MMKV → l'ancienne valeur reste intacte et le fichier n'est jamais corrompu (edge held-out, AC #9) — non prouvable en unitaire sans instrumentation FS ; vérifié sur device au plan 01-07 (kill pendant l'écriture du réglage) et couvert par l'atomicité MMKV documentée en threat T-01-05d"
      verification: backstop
  artifacts:
    - path: "src/core/storage/mmkv.ts"
      provides: "createMMKV x2 : instance standard 'settings' + instance chiffrée 'entitlements' (path explicite + encryptionKey + AES-256)"
    - path: "src/core/storage/secureStorage.ts"
      provides: "loadOrCreateEntitlementsKey — génération expo-crypto, stockage SecureStore, politique fail-hard (jamais régénérer)"
    - path: "src/core/storage/zustandAdapter.ts"
      provides: "Adaptateur StateStorage sur MMKV (getString/set/remove — API v4) + createJSONStorage"
    - path: "src/stores/settingsStore.ts"
      provides: "Store persist standard + seed du contrat D-007 complet"
    - path: "src/stores/entitlementsStore.ts"
      provides: "Store persist sur l'instance chiffrée uniquement"
    - path: "src/i18n/index.ts"
      provides: "initReactI18next statique EN/FR, fallbackLng en, namespaces D-030"
    - path: "src/i18n/locales/{en,fr}/common.json"
      provides: "Clés minimales Phase 1 (D-030 : un dossier par langue, namespace common peuplé, structure prête pour tracking/settings/exports/crash)"
    - path: "tests/entitlements-encrypted-instance.test.ts"
      provides: "Test de wiring prohibition R4 : store branché instance chiffrée ; fixture violation = branchement instance standard"
  key_links:
    - from: "src/stores/entitlementsStore.ts"
      to: "src/core/storage/mmkv.ts"
      via: "createJSONStorage sur l'instance chiffrée (encryptionKey SecureStore) — jamais l'instance standard"
      pattern: "mmkvEncrypted|entitlements"
    - from: "src/stores/settingsStore.ts"
      to: "src/db/schemas/settings.ts"
      via: "Seed du contrat D-007 depuis les constantes/défauts du schéma feuille (direction D-041 : stores importent schemas)"
      pattern: "from '@/db/schemas/settings"
    - from: "src/i18n/index.ts"
      to: "src/i18n/locales/"
      via: "resources statiques importés au bundling — aucune initialisation réseau"
      pattern: "fallbackLng"
  prohibitions:
    - statement: "MUST NOT stocker entitlements/état d'achat hors de l'instance MMKV chiffrée (jamais instance standard, jamais SQLite, jamais plaintext)"
      status: resolved
      verification: test — tests/entitlements-encrypted-instance.test.ts prouve le wiring du store entitlements sur l'instance chiffrée ; violation fixture = store témoin branché sur l'instance standard (doit faire échouer l'assertion de wiring), clean fixture = le wiring chiffré réel
---

<objective>
Poser la couche de stockage clé-valeur et le boot i18n : les deux instances MMKV v4 (standard settings/trial + chiffrée entitlements avec clé SecureStore et path explicite), l'adaptateur Zustand persist, le settings store seedant le contrat complet D-007, l'entitlements store sur l'instance chiffrée uniquement, et l'init i18next EN/FR statique avec fallback EN.

Purpose: SPEC R4 + R7 (AC #5/#6/#11 + backstop concurrence MMKV) — Phase 4 branchera la logique trial/entitlements sur ces instances sans migration ; Phase 3 branchera l'UI nuit sur les clés seedées sans toucher au schéma MMKV.
Output: 3 modules storage, 2 stores, boot i18n + 2 JSON de locale, 3 fichiers de tests verts.
</objective>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
docs/05-data-model.md (défauts settings lignes 1931-1933 ; backupSettingsSchema lignes 1963-1965 ; clés MMKV exportées/exclues lignes 2015-2036)
src/db/schemas/settings.ts (créé en 01-03 — contrat D-007 à consommer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instances MMKV v4 + clé SecureStore fail-hard</name>
  <files>src/core/storage/mmkv.ts, src/core/storage/secureStorage.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 2 verbatim : createMMKV({id, path?, encryptionKey, encryptionType}) ; Anti-Patterns : régénérer la clé est interdit ; Open Questions (RESOLVED) Q2 : path explicite pour les deux instances)
    - docs/05-data-model.md lignes 2015-2036 (l'instance standard porte settings:* et plus tard trial:* ; les secrets/entitlements ne vivent QUE côté chiffré)
    - node_modules/react-native-mmkv/README ou types (API v4 : createMMKV, getString/set/remove/contains/getAllKeys — la suppression est remove(), pas l'ancienne méthode v3)
  </read_first>
  <action>Créer src/core/storage/secureStorage.ts : constante du nom de clé SecureStore (ex. babylog.mmkv.entitlements.key), fonction async loadOrCreateEntitlementsKey — lecture via expo-secure-store getItemAsync ; si présente, retourner telle quelle ; si absente, générer 32 octets via expo-crypto getRandomBytes encodés base64 et stocker via setItemAsync puis retourner ; en cas d'ERREUR DE LECTURE (autre qu'absence), throw immédiatement — la clé n'est JAMAIS régénérée sur erreur (régénérer détruirait silencieusement les entitlements chiffrés ; la recovery est une décision humaine, Pitfall/Anti-Pattern RESEARCH). Créer src/core/storage/mmkv.ts : les deux instances via createMMKV v4 — (1) instance standard id 'settings' (settings:* aujourd'hui, trial:* en Phase 4) ; (2) instance chiffrée id 'entitlements' avec path explicite sous le répertoire Application Support de l'app (résolu via l'API nouvelle expo-file-system — Paths, jamais les fonctions legacy qui throw en SDK 57) + encryptionKey issue de secureStorage + encryptionType 'AES-256'. Le path explicite rend les cibles d'exclusion iCloud déterministes (le sweep 01-06 énumérera ces répertoires). Exporter les deux instances et une factory async qui construit l'ensemble après chargement de la clé (le boot 01-07 l'attendra) ; exposer le path de chaque instance pour le sweep. Documenter en tête que l'instance standard est backup-éligible côté fichier mais ne contient que des réglages non sensibles (le contrat de clés excluant secrets/billing/ai/analytics, docs/05 lignes 2027-2036).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit (types MMKV v4 ou expo-file-system nouvelle API mal employés)</fails_when>
    <automated>grep -n "encryptionType" src/core/storage/mmkv.ts | grep -c "AES-256"</automated>
    <fails_when>sortie 0 (l'instance chiffrée ne déclare pas AES-256)</fails_when>
  </verify>
  <acceptance_criteria>
    - mmkv.ts crée exactement deux instances : 'settings' standard et 'entitlements' chiffrée AES-256 avec path explicite + clé SecureStore
    - secureStorage.ts throw sur erreur de lecture SecureStore et ne régénère la clé que si absente (jamais sur erreur)
    - La clé fait 32 octets d'entropie expo-crypto, stockée sous un nom de clé dédié
    - Aucune API expo-file-system legacy (les fonctions classiques throw en SDK 57 — uniquement Paths/File/Directory)
  </acceptance_criteria>
  <done>Deux instances MMKV prêtes, clé SecureStore fail-hard — socle des stores et du sweep iCloud.</done>
  <reversibility rating="costly">Le schéma MMKV (ids d'instances, clés, path) est figé par D-007 — renommer après coup imposerait une migration de données MMKV.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Adaptateur Zustand + settingsStore (seed D-007) + entitlementsStore chiffré + tests</name>
  <files>src/core/storage/zustandAdapter.ts, src/stores/settingsStore.ts, src/stores/entitlementsStore.ts, tests/stores/settings-persist.test.ts, tests/entitlements-encrypted-instance.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 2 : adaptateur StateStorage verbatim — getItem/setItem/removeItem ; zustand 5 createJSONStorage/persist)
    - src/db/schemas/settings.ts (01-03 — constantes de clés, défauts auto/1200/420, contrat D-007)
    - .planning/phases/01-foundation-data-layer/01-SPEC.md (R4 : entitlements QUE sur l'instance chiffrée + descripteur du test de wiring + backstop concurrence)
    - docs/05-data-model.md lignes 2015-2036 (clés exportées vs exclues — l'instance entitlements est hors backup)
  </read_first>
  <action>Créer src/core/storage/zustandAdapter.ts : factory prenant une instance MMKV et retournant un StateStorage (getItem via getString ?? null, setItem via set, removeItem via remove — l'API de suppression v4) à brancher dans createJSONStorage. Créer src/stores/settingsStore.ts : store zustand persist nommé 'settings' sur l'instance standard via l'adaptateur, état typé par le contrat du schéma feuille, avec seed du contrat COMPLET D-007 au premier lancement (night_mode défaut auto, night_mode_start_minutes 1200, night_mode_end_minutes 420 — valeurs docs/05 lignes 1931-1933) ; aucune clé trial:* n'est seedée en Phase 1 ; exposer une action de mise à jour typée par clé. Créer src/stores/entitlementsStore.ts : store persist nommé 'entitlements' construit UNIQUEMENT sur l'instance chiffrée via le même adaptateur — état minimal Phase 1 (conteneur vide typé, la logique métier arrive en Phase 4) ; le wiring est le livrable (SPEC R4). Créer tests/stores/settings-persist.test.ts : écriture d'un réglage → relecture via une instance fraîche (simulate kill/relaunch) retrouve la valeur ; double écriture de la même clé → la dernière valeur gagne au relaunch ; le seed pose les trois clés aux défauts D-007 et ne crée aucune clé trial. Créer tests/entitlements-encrypted-instance.test.ts : le test de wiring de la prohibition R4 — assert que l'adaptateur du store entitlements est branché sur l'instance chiffrée (id + encryptionKey présents, instance standard absente du wiring) ; fixture violation : un store témoin branché sur l'instance standard doit faire échouer l'assertion de wiring (preuve que le test détecte le mauvais branchement) ; le store n'écrit jamais en SQLite (aucun import repository). Stratégie de mock obligatoire pour que les deux fichiers se chargent sous vitest Node pur (react-native-mmkv/Nitro et expo-secure-store sont natifs, ils ne se chargent pas sous Node — RESEARCH Test Map « unit (mock storage) ») : en tête de chaque fichier de test, vi.mock('react-native-mmkv') et vi.mock('expo-secure-store') HOISTÉS avant tout import (aucune chaîne native, y compris react-native-nitro-modules transitif, n'est chargée) ; le double MMKV est Map-backed, expose createMMKV(options) qui ENREGISTRE les options (id, path, encryptionKey, encryptionType) et implémente getString/set/remove/contains/getAllKeys sur un backing store module-scope partagé — la relecture « instance fraîche » du kill/relaunch simulé construit un second store sur une nouvelle instance du double lisant ce même backing store ; le double SecureStore est une KV en mémoire (et expo-crypto mocké en octets déterministes pour la clé). Le test de wiring lit les options ENREGISTRÉES par createMMKV (id 'entitlements' + encryptionKey + AES-256 pour l'instance chiffrée) — la fixture violation, store branché sur l'instance standard, échoue sur ces options enregistrées.</action>
  <verify>
    <automated>npx vitest run tests/stores/settings-persist.test.ts tests/entitlements-encrypted-instance.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou "0 passed" dans le résumé</fails_when>
  </verify>
  <acceptance_criteria>
    - settings-persist vert : survie kill/relaunch simulée par relecture sur instance fraîche + double écriture last-wins (AC #5)
    - Le seed pose settings:night_mode=auto, settings:night_mode_start_minutes=1200, settings:night_mode_end_minutes=420 et aucune clé trial (D-007-ctx)
    - entitlements-encrypted-instance vert sur le wiring chiffré et rouge sur la fixture violation (store témoin sur instance standard)
    - zustandAdapter utilise getString/set/remove (API v4) et createJSONStorage
  </acceptance_criteria>
  <done>Stores persistants opérationnels, contrat D-007 seedé, wiring chiffré prouvé par test — le backstop concurrence est documenté par le test held-out.</done>
</task>

<task type="auto">
  <name>Task 3: Boot i18next EN/FR statique + tests fallback/UTF-8</name>
  <files>src/i18n/index.ts, src/i18n/locales/en/common.json, src/i18n/locales/fr/common.json, tests/i18n/fallback.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pitfall 8 : passer les JSON bundlés à l'init, fallbackLng en, initReactI18next ; i18next est pure JS — testable dans vitest)
    - .planning/phases/01-foundation-data-layer/01-CONTEXT.md (D-030 : namespaces par domaine common/tracking/settings/exports/crash en en/ et fr/ ; common minimal Phase 1)
    - .planning/phases/01-foundation-data-layer/01-SPEC.md (R7 : zéro fetch, clé FR absente → string EN)
  </read_first>
  <action>Créer la structure src/i18n/locales/en/ et fr/ avec common.json minimal de chaque côté (quelques clés réelles : titre app, libellés du futur écran skeleton — les namespaces tracking/settings/exports/crash restent des dossiers à créer par leurs phases, la structure y est prête). Créer src/i18n/index.ts : import statique des deux JSON (bundlés au build — aucun backend http, aucun chargement dynamique réseau), initReactI18next avec resources {en:{common},fr:{common}}, fallbackLng 'en', lng résolu au boot via expo-localization (première locale du téléphone, restreinte à fr sinon en), interpolation par défaut, et export d'une fonction initI18n() idempotente appelée par le boot 01-07. Créer tests/i18n/fallback.test.ts (i18next est pure JS — tourne dans vitest) : une clé présente en EN et absente en FR rend la string EN (jamais la clé brute, jamais un crash) ; une clé présente en FR rend la string FR ; l'init est synchrone depuis les resources importées (aucun loader réseau — assert que t() résout immédiatement après init sans callback async) ; round-trip UTF-8/emoji : des clés/valeurs FR contenant accents français, emoji et caractères multi-octets rendus identiques par t() (moitié i18n de l'AC #15). Stratégie de mock obligatoire (chargement sous vitest Node pur — expo-localization est natif) : vi.mock('expo-localization') HOISTÉ en tête du fichier de test, getLocales retournant une locale fixe pour chaque scénario (fr puis en) — équivalent acceptable : initI18n(localeOverride?) accepte un paramètre que le test passe et qui court-circuite la détection ; aucun import expo-localization non mocké n'arrive au chargement du module.</action>
  <verify>
    <automated>npx vitest run tests/i18n/fallback.test.ts</automated>
    <fails_when>non-zero exit, tout test en échec, ou une clé brute rendue au lieu de la string EN de fallback</fails_when>
  </verify>
  <acceptance_criteria>
    - Clé absente en FR → string EN rendue (jamais la clé brute) — AC #11
    - Init synchrone depuis resources statiques importés — zéro fetch possible (aucun import de backend http)
    - fallbackLng 'en' + lng depuis expo-localization restreint à en/fr
    - Round-trip emoji/accents FR identique via t() — AC #15 moitié i18n
    - Structure de dossiers D-030 (en/ fr/ avec common.json ; namespaces futurs tracés)
  </acceptance_criteria>
  <done>i18n bootée EN/FR statique avec fallback EN prouvé par test — Phase 3 ajoutera ses clés sans toucher l'infra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SecureStore (Keychain/Keystore) → clé MMKV | La clé de chiffrement des entitlements traverse cette frontière au boot |
| MMKV chiffrée ← lecture processus | Seule frontière de stockage des entitlements (jamais SQLite, jamais clair) |
| bundle → i18next | JSON de traduction statiques bundlés — pas de source distante |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05a | Elevation of Privilege / Tampering | Entitlements stockés hors instance chiffrée | high | mitigate | Instance chiffrée AES-256 à clé SecureStore uniquement ; test de wiring prohibition R4 avec fixture violation (branchement standard détecté) ; aucun import repository dans le store |
| T-01-05b | Denial of Service | Clé MMKV régénérée sur erreur SecureStore | high | mitigate | Politique fail-hard : throw sur erreur de lecture, régénération uniquement si clé absente — jamais silencieuse (un reset détruirait les entitlements) |
| T-01-05c | Information Disclosure | Entitlements en clair sur disque | medium | mitigate | encryptionType AES-256 v4 + clé 32 octets expo-crypto hors du bundle, path explicite hors Documents par défaut |
| T-01-05d | Tampering | Kill pendant écriture MMKV (fichier corrompu) | medium | mitigate | Atomicité garantie par MMKV — backstop : l'ancienne valeur reste intacte si l'écriture est interrompue (edge held-out, non re-testable en unitaire sans instrumentation FS ; vérification device 01-07) |
| T-01-05e | Information Disclosure | i18n chargeant des traductions distantes | low | mitigate | Resources statiques importés au bundling ; test assert la résolution synchrone sans loader |
</threat_model>

<verification>
- npx vitest run tests/stores tests/entitlements-encrypted-instance.test.ts tests/i18n vert
- Seed D-007 complet (auto/1200/420, zéro clé trial)
- Wiring chiffré prouvé + fixture violation détectée
- Fallback EN + round-trip UTF-8 FR prouvés
</verification>

<success_criteria>
La couche stockage existe et est prouvée : dual-instance MMKV v4 (standard + chiffrée SecureStore fail-hard), adaptateur Zustand, settings seedés au contrat complet D-007, entitlements exclusivement chiffrés (prohibition testée), i18n EN/FR statique avec fallback — prêtes pour le boot 01-07 et les phases 3/4 sans migration.
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-05-SUMMARY.md` quand done
</output>
