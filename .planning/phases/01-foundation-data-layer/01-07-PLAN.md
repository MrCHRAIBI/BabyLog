---
phase: 01-foundation-data-layer
plan: 07
type: execute
wave: 6
depends_on: ["01-06"]
files_modified:
  - src/app/_layout.tsx
  - src/app/index.tsx
autonomous: false
requirements: [OPS-02]
user_setup:
  - service: physical-android
    why: "Vérification AC device Android 14 permanente (D-002-ctx)"
    env_vars: []
    dashboard_config:
      - task: "Activer le débogage USB et autoriser l'ordinateur sur l'Android 14"
        location: "Paramètres Android → Options développeur → Débogage USB"
  - service: expo-eas
    why: "Dev build iOS EAS cloud (D-001-ctx) — compte Expo requis pour eas build"
    env_vars: []
    dashboard_config:
      - task: "eas login (compte Expo) — requis avant tout build EAS"
        location: "Terminal — npx eas-cli login"
  - service: apple-developer
    why: "UAT iOS : enrollment Apple Developer Program approuvé AVANT l'UAT (D-036/PR-2) + UDID iPhone 12 enregistré (D-035)"
    env_vars: []
    dashboard_config:
      - task: "Confirmer l'approbation de l'enrollment Apple Developer (D-036) — précondition bloquante de l'UAT iOS"
        location: "developer.apple.com → account → membership"
      - task: "Enregistrer l'UDID de l'iPhone 12 via eas device:create (D-035) — Apple traite sous 24-72 h"
        location: "Terminal — npx eas-cli device:create (QR/URL à ouvrir sur l'iPhone)"
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Au boot, _layout exécute la chaîne complète : openDatabaseAsync → PRAGMAs docs/05 → runMigrations → sweep backupGuard → clé SecureStore → instances MMKV → seed settings → initI18n → splash hidden — avant tout rendu d'écran"
    - "Depuis l'écran d'accueil du dev build, un parent crée un profil (prénom + date de naissance) et le voit s'afficher — la donnée passe par le schéma Zod, expo-crypto randomUUID, babyProfileRepository.create puis listProfiles (Walking Skeleton, RESEARCH Pattern 4)"
    - "Sur l'Android 14 physique : le dev build local se lance sans erreur de module natif (expo-sqlite, MMKV, quick-crypto, expo-iap chargés — pas Expo Go) et aucun appel réseau n'apparaît au boot (AC #1)"
    - "Sur l'Android 14 physique : modifier le réglage nuit via le contrôle du skeleton, killer l'app, la relancer → la valeur persiste via la chaîne Zustand + MMKV (AC #5)"
    - "Le manifest mergé de l'APK Android porte allowBackup=false et dataExtractionRules (aapt2 dump xmltree) (AC #7, OPS-02)"
    - "Sur l'iPhone 12 (dev build EAS ad-hoc) : le read-back in-app montre isExcludedFromBackup=true sur babylog.db, -wal, -shm et tous les fichiers MMKV (AC #8 — preuve in-app RESEARCH Open Question 1)"
  artifacts:
    - path: "src/app/_layout.tsx"
      provides: "Boot chain complète + ErrorBoundary racine + import global.css NativeWind"
    - path: "src/app/index.tsx"
      provides: "Écran walking-skeleton : création + liste de profils via la chaîne repository réelle + contrôle de réglage MMKV (véhicule AC #5)"
  key_links:
    - from: "src/app/_layout.tsx"
      to: "src/core/database/client.ts"
      via: "Boot : ouverture + PRAGMAs + runMigrations avant hideAsync du splash"
      pattern: "runMigrations|openDatabase"
    - from: "src/app/index.tsx"
      to: "src/db/repositories/babyProfileRepository.ts"
      via: "Create + list par le repository réel (Zod + randomUUID) — aucune écriture SQL dans l'écran"
      pattern: "babyProfileRepository"
    - from: "src/app/index.tsx"
      to: "src/stores/settingsStore.ts"
      via: "Lecture/écriture settings:night_mode via le store persisté (véhicule AC kill/relaunch)"
      pattern: "useSettingsStore"
---

<objective>
Compléter le Walking Skeleton et le prouver sur les deux devices : brancher la chaîne de boot complète dans _layout (DB → migrations → exclusions backup → MMKV chiffré → seed D-007 → i18n), écrire l'écran skeleton (création + lecture de profil par la vraie chaîne repository, contrôle de réglage réel comme véhicule AC #5), puis vérifier sur l'Android 14 physique (build local) et l'iPhone 12 (dev build EAS ad-hoc, préconditions D-035/D-036).

Purpose: SPEC R1 + ACs device #1/#5/#7/#8 — la fondation n'est « production-grade » que si elle tourne sur du vrai matériel sans qu'aucune donnée ne quitte l'appareil ; c'est aussi la preuve d'architecture que les phases 2-7 construiront sans la retoucher.
Output: _layout + index finaux, SUMMARY avec preuves device (screenshots/logcat/aapt2/read-back), ACs device cochées.
</objective>

<execution_context>
@D:/projets/react/BabyLog/.zcode/gsd-core/workflows/execute-plan.md
@D:/projets/react/BabyLog/.zcode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-data-layer/01-SPEC.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/SKELETON.md
docs/06-ui-ux-design.md (tokens pour l'écran skeleton)
src/app/_layout.tsx, src/app/index.tsx (minimaux de 01-01 — à étendre, pas à réinventer)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Walking skeleton — boot chain complète + écran profil réel (une seule voie, bout en bout)</name>
  <files>src/app/_layout.tsx, src/app/index.tsx</files>
  <read_first>
    - src/app/_layout.tsx et src/app/index.tsx (minimaux 01-01 — patterns à étendre)
    - .planning/phases/01-foundation-data-layer/01-RESEARCH.md (Pattern 4 Walking Skeleton verbatim ; Pattern 2 boot ordering : SecureStore async → splash gate ; Pitfall 6 : smoke modules natifs en dev build uniquement)
    - src/core/database/client.ts, src/core/database/migrations/runner.ts (01-03), src/core/backupGuard/index.ts (01-06), src/core/storage/secureStorage.ts + mmkv.ts (01-05), src/i18n/index.ts (01-05), src/core/errors/ErrorBoundary.tsx (01-06)
    - src/db/schemas/babyProfile.ts + src/db/repositories/babyProfileRepository.ts (01-03/01-04 — contrat create/list)
  </read_first>
  <action>Réécrire src/app/_layout.tsx comme le point de boot unique : import '@/global.css' (NativeWind) ; SplashScreen.preventAutoHideAsync() en scope module ; une fonction boot() async qui enchaîne DANS CET ORDRE — ouverture DB + PRAGMAs via le client 01-03, runMigrations, backupGuard.sweepAfterBoot(), loadOrCreateEntitlementsKey + construction des instances MMKV (settings + entitlements chiffrée), seed du settings store (contrat D-007, idempotent), initI18n() — puis SplashScreen.hideAsync() ; le composant racine attend la fin du boot (état local) avant de rendre les enfants, enveloppés dans l'ErrorBoundary racine 01-06 ; aucune ligne réseau nulle part. Réécrire src/app/index.tsx comme l'écran du Walking Skeleton (RESEARCH Pattern 4) : libellés via t() (namespace common), formulaire minimal de création de profil (prénom, date de naissance) dont la soumission construit un id via expo-crypto randomUUID, valide par le schéma d'insert babyProfile de 01-03 puis appelle babyProfileRepository.create, puis rafraîchit la liste par babyProfileRepository.listProfiles et rend les profils existants (nom + date) — AUCUN SQL dans l'écran (prohibition R3) ; plus un contrôle réel de réglage (segmenté off/on/auto) lié à settings:night_mode via useSettingsStore — c'est le véhicule manuel de l'AC #5 kill/relaunch (l'UI de réglages finale est le travail Phase 3, ce contrôle prouve la chaîne d'écriture MMKV réelle sans changement architectural). Un profil créé persiste dans babylog.db : tuer et relancer l'app le ré-affiche (prouve la chaîne SQLite complète). Cette voie unique — tap → Zod → repository → SQLite → relecture → rendu, et réglage → MMKV — est la production-quality à conserver : les phases suivantes ne remplaceront aucun maillon.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit (contrats 01-02..01-06 mal consommés)</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit ou tout test en échec (la suite phase doit rester verte après le wiring)</fails_when>
    <automated>grep -c "babyProfileRepository" src/app/index.tsx</automated>
    <fails_when>sortie 0 (l'écran ne passe pas par la chaîne repository réelle)</fails_when>
  </verify>
  <acceptance_criteria>
    - _layout enchaîne boot complet dans l'ordre : DB+PRAGMAs → migrations → sweep → clé SecureStore → MMKV → seed → i18n → hide splash, puis ErrorBoundary autour des enfants
    - index crée et liste un profil via le repository réel (randomUUID + Zod + listProfiles), sans une seule chaîne SQL dans l'écran
    - Le contrôle night_mode écrit via useSettingsStore (instance MMKV standard persistée)
    - Aucune dépendance réseau nulle part dans les deux fichiers (audit zero-cloud 01-02 reste vert)
    - Suite vitest complète verte + tsc strict vert
  </acceptance_criteria>
  <done>La voie skeleton — profil et réglage — est complète et compile ; reste la preuve device (Tasks 2-3).</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: ACs device Android 14 — dev build local, boot propre, persistance, manifest</name>
  <precondition>Android 14 physique connecté avec débogage USB actif — adb devices liste l'appareil (user_setup, D-002-ctx).</precondition>
  <files>(aucun — vérification device ; le SUMMARY journalise les preuves)</files>
  <what-built>Le Walking Skeleton complet (Task 1) déployé sur l'Android 14 physique via le build local D-001-ctx.</what-built>
  <how-to-verify>1. Exécuter npm run android:dev (expo run:android) avec l'appareil branché — le build natif local compile et installe. 2. AC #1 : l'app se lance SANS Expo Go, aucun log d'erreur de module natif ; vérifier dans logcat qu'AUCUN appel réseau n'est émis au boot (filtrer les paquets/connexions — zéro). 3. AC skeleton : créer un profil (prénom + date) sur l'écran → il s'affiche dans la liste ; tuer l'app (swipe/force-stop), relancer → le profil est toujours là (chaîne SQLite complète). 4. AC #5 : changer le réglage nuit via le contrôle du skeleton, forcer le kill de l'app PENDANT ou juste après l'écriture, relancer → la valeur choisie est restaurée (atomicité MMKV — edge backstop AC #9) ; refaire un second changement → la dernière valeur gagne. 5. AC #7 / OPS-02 : localiser l'APK produit (android/app/build/outputs/apk/), exécuter aapt2 dump xmltree <apk> AndroidManifest.xml (aapt2 présent dans ANDROID_HOME/build-tools) et vérifier allowBackup=(0x0)false et l'attribut dataExtractionRules=@xml/data_extraction_rules ; décompiler/res/du manifest pour confirmer le XML d'exclusion. 6. Journaliser chaque preuve (commandes + sorties + captures) dans le SUMMARY de ce plan. Tout écart → ne PAS cocher l'AC : diagnostiquer, corriger dans le code source (pas de contournement), re-vérifier.</how-to-verify>
  <resume-signal>Taper "approved" quand toutes les ACs Android (#1 partie Android, #5, #7, #9 backstop) sont prouvées et journalisées — ou décrire l'écart constaté</resume-signal>
  <done>AC #1 (partie Android), #5, #9 (backstop) et #7 prouvées sur l'Android 14 physique avec preuves journalisées dans le SUMMARY.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: ACs device iOS — dev build EAS ad-hoc + exclusion iCloud (read-back)</name>
  <precondition>Conditions D-003-ctx réunies : (1) enrollment Apple Developer Program approuvé (D-036) — eas whoami fonctionne et le compte Expo est loggé ; (2) UDID de l'iPhone 12 enregistré via npx eas-cli device:create (D-035) — visible dans eas device:list ; (3) l'iPhone 12 est physiquement disponible pour la session UAT (D-002-ctx). Si l'iPhone est indisponible : appliquer D-004-ctx — replanifier la vérification dans la fenêtre de la phase avec un todo explicite dans STATE.md, JAMAIS waivée, JAMAIS remplacée par un simulateur.</precondition>
  <files>(aucun — vérification device ; le SUMMARY journalise les preuves)</files>
  <what-built>Le même Walking Skeleton déployé sur l'iPhone 12 via le dev build EAS cloud (profil development, distribution internal — D-001-ctx : pas de Xcode sous Windows).</what-built>
  <how-to-verify>1. Lancer npx eas-cli build --profile development --platform ios (le module local backup-guard et les pods se compilent dans le cloud) ; attendre le build URL. 2. Installer sur l'iPhone 12 via le lien de build (installation ad-hoc — le UDID D-035 doit déjà être dans le profil ; si "device not in provisioning profile" : re-générer le profil avec eas-cli >= 19.1.0 --refresh-ad-hoc-provisioning-profile, cf. Pitfall 7). 3. AC #1 (partie iOS) : l'app se lance sans erreur de module natif ; zéro réseau au boot. 4. AC #8 : déclencher la fonction de read-back exportée par src/core/backupGuard (01-06) — via une entrée de dev temporaire sur l'écran skeleton ou une commande de boot en __DEV__ — et vérifier isExcludedFromBackup=true pour babylog.db, babylog.db-wal, babylog.db-shm et TOUS les fichiers des répertoires MMKV (le read-back interroge exactement le flag que backupd consulte — RESEARCH Open Questions (RESOLVED) Q1 : preuve in-app acceptable ; si une inspection container complète est exigée plus tard, la replanifier avec libimobiledevice hors Phase 1). 5. Refaire le cycle profil + réglage kill/relaunch sur iOS (parité de la chaîne). 6. Journaliser commandes, sorties et captures dans le SUMMARY. Un flag à false sur un seul fichier = AC #8 non satisfaite → corriger le sweep (ordre boot / fichiers manqués) et re-vérifier — jamais cocher par optimisme.</how-to-verify>
  <resume-signal>Taper "approved" quand AC #8 (et la parité iOS du boot) est prouvée et journalisée — ou "replan D-004-ctx" si l'iPhone est indisponible — ou décrire l'écart constaté</resume-signal>
  <done>AC #1 (partie iOS) et AC #8 prouvées sur l'iPhone 12 réel avec preuves journalisées ; ou replanification D-004-ctx actée dans STATE.md avec todo explicite si le device est indisponible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device physique / logcat | Preuves AC — lecture seule, aucune donnée n'en sort |
| EAS cloud → artefact iOS signé | Le build cloud produit l'artefact ad-hoc installé sur l'iPhone |
| OS backup channels (Google/iCloud) | Canaux d'exfiltration passifs — vérifiés fermés ici |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07a | Information Disclosure | Appel réseau au boot (fuite potentielle de métadonnées) | high | mitigate | AC #1 : logcat Android sans aucune connexion sortante au boot ; zéro dépendance réseau (audit 01-02) ; tout log de connexion = AC non satisfaite |
| T-01-07b | Information Disclosure | Fichiers de données dans l'iCloud backup | high | mitigate | AC #8 : read-back in-app isExcludedFromBackup=true sur DB + sidecars + MMKV (module 01-06) ; todo D-004-ctx si device indisponible — jamais waivée, jamais simulateur |
| T-01-07c | Tampering | Artefact EAS modifié en transit/installation | medium | mitigate | Distribution internal ad-hoc signée Apple (profil UDID D-035) ; build URL officielle EAS uniquement |
| T-01-07d | Information Disclosure | Auto-backup Google du DB/MMKV | high | mitigate | AC #7 : aapt2 dump xmltree prouve allowBackup=false + dataExtractionRules dans le manifest MERGÉ de l'APK réel (pas la config source) |
| T-01-07e | Repudiation | AC cochée sans preuve | medium | mitigate | Chaque AC device journalise commande + sortie + capture dans le SUMMARY ; un écart bloque la coche et impose un correctif source |
</threat_model>

<verification>
- tsc + suite vitest complète verts après le wiring boot/écran
- Android 14 : boot sans erreur native, zéro réseau au boot, profil + réglage survivent au kill/relaunch, aapt2 confirme allowBackup=false + dataExtractionRules
- iPhone 12 : boot EAS ad-hoc, read-back isExcludedFromBackup=true sur tous les fichiers cibles
- Preuves device journalisées dans 01-07-SUMMARY.md (les ACs #1/#5/#7/#8/#9 passent de « non vérifiée » à « prouvée »)
</verification>

<success_criteria>
Le Walking Skeleton tourne sur les deux devices physiques : profil créé et relu par la chaîne repository réelle, réglage persistant via MMKV, zéro réseau au boot, exclusions de backup prouvées des deux côtés — la fondation Phase 1 est prouvée de bout en bout et les phases 2-7 peuvent construire dessus sans retoucher l'architecture.
</success_criteria>

<output>
Créer `.planning/phases/01-foundation-data-layer/01-07-SUMMARY.md` quand done
</output>
