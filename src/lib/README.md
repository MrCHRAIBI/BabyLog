# Lib (docs/04)

- `constants.ts` — clés MMKV, limites free tier, IDs produits stores
- `config.ts` — flags : NIGHT_MODE, FREE_LIMITS, AD_CAPS
- `i18n/` — init i18next + locales en/fr/es/it/ja (fallback `en`, clés typées via `i18next.d.ts`)
